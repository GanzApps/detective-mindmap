---
phase: 07-export
plan: 03
type: execute
wave: 3
depends_on:
  - "07-01"
  - "07-02"
files_modified:
  - src/components/layout/CaseHeader.tsx
  - src/components/layout/CaseWorkspaceShell.tsx
  - src/components/pages/CaseWorkspacePage.tsx
  - src/components/graph/GraphWorkspace.tsx
autonomous: true
requirements:
  - EXPORT-01
  - EXPORT-02

requirements_addressed:
  - EXPORT-01
  - EXPORT-02

must_haves:
  truths:
    - "The single export entry point opens a small menu rather than triggering one fixed action"
    - "The selected menu action routes to the active renderer export target"
    - "Export wiring stays compact and consistent with the workspace header"
  artifacts:
    - path: "src/components/layout/CaseHeader.tsx"
      provides: "Single export entry point with small choice menu"
      contains: "Export Report"
    - path: "src/components/pages/CaseWorkspacePage.tsx"
      provides: "Case-level export orchestration"
      contains: "reportExporter"
---

<objective>
Wire the export UX into the workspace.

Purpose: Export should feel like a natural extension of the current header and case workspace, with a single compact entry point that routes to the right output path.
Output: menu-based export UX wired from header to active renderer export orchestration.
</objective>

<context>
@.planning/phases/07-export/07-CONTEXT.md
@.planning/phases/07-export/07-RESEARCH.md
@src/components/layout/CaseHeader.tsx
@src/components/layout/CaseWorkspaceShell.tsx
@src/components/pages/CaseWorkspacePage.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Evolve the header export button into a compact menu surface</name>
  <files>src/components/layout/CaseHeader.tsx</files>
  <action>
Replace the static Export Report button behavior with a compact menu that lets the user choose PNG, PDF, or both while preserving the shell's visual language.
  </action>
  <verify>
    <automated>Header exposes one export entry point with a small output-choice menu</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 2: Wire export actions through the workspace shell and page orchestration</name>
  <files>src/components/layout/CaseWorkspaceShell.tsx, src/components/pages/CaseWorkspacePage.tsx</files>
  <action>
Thread export callbacks through the shell/page boundary so export intent can combine current case metadata from the store with the active renderer export target.
  </action>
  <verify>
    <automated>Case workspace orchestration can trigger export actions with active case context</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 3: Connect GraphWorkspace export access to the UI flow</name>
  <files>src/components/graph/GraphWorkspace.tsx</files>
  <action>
Expose the active renderer export access path to the page-level export flow without disturbing the mounted dual-renderer architecture.
  </action>
  <verify>
    <automated>Export UI can resolve the active renderer target through GraphWorkspace</automated>
  </verify>
</task>

</tasks>

<success_criteria>
- Export stays behind one compact menu-based entry point
- Export actions receive current case and renderer context
- UI wiring preserves the existing workspace composition patterns
</success_criteria>

<output>
After completion, create `.planning/phases/07-export/07-03-SUMMARY.md`.
</output>
