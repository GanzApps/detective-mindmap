---
phase: 07-export
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/graph/ForceGraph2D.tsx
  - src/components/graph/MindMap3D.tsx
  - src/components/graph/GraphWorkspace.tsx
autonomous: true
requirements:
  - EXPORT-01
  - EXPORT-03

requirements_addressed:
  - EXPORT-01
  - EXPORT-03

must_haves:
  truths:
    - "Both renderers expose explicit current-view export handles"
    - "Current-view capture can force a fresh draw without resetting user state"
    - "GraphWorkspace can request export data from the active renderer only"
  artifacts:
    - path: "src/components/graph/ForceGraph2D.tsx"
      provides: "2D renderer export handle"
      contains: "useImperativeHandle"
    - path: "src/components/graph/MindMap3D.tsx"
      provides: "3D renderer export handle"
      contains: "useImperativeHandle"
---

<objective>
Create export-ready renderer handles for the current graph view.

Purpose: Export should snapshot exactly what the user sees in the active renderer, so both graph surfaces need a small imperative export contract before utilities or UI wiring can rely on them.
Output: active-renderer export handles exposed through GraphWorkspace.
</objective>

<context>
@.planning/phases/07-export/07-CONTEXT.md
@.planning/phases/07-export/07-RESEARCH.md
@src/components/graph/GraphWorkspace.tsx
@src/components/graph/ForceGraph2D.tsx
@src/components/graph/MindMap3D.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add an export handle to ForceGraph2D</name>
  <files>src/components/graph/ForceGraph2D.tsx</files>
  <action>
Expose the current 2D canvas and a redraw-before-capture path through a small imperative handle so PNG/PDF utilities can capture the exact active view without restarting the simulation.
  </action>
  <verify>
    <automated>2D renderer export handle preserves current zoom/pan/selection state</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 2: Add an export handle to MindMap3D</name>
  <files>src/components/graph/MindMap3D.tsx</files>
  <action>
Expose the current 3D canvas and a redraw-before-capture path through a small imperative handle so export preserves the live camera view and highlighted state.
  </action>
  <verify>
    <automated>3D renderer export handle preserves current camera/selection state</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 3: Surface the active export target through GraphWorkspace</name>
  <files>src/components/graph/GraphWorkspace.tsx</files>
  <action>
Connect both renderer handles to GraphWorkspace and provide a single active-renderer export access path that higher-level export orchestration can consume.
  </action>
  <verify>
    <automated>GraphWorkspace can resolve the active renderer export target without unmounting or resetting either renderer</automated>
  </verify>
</task>

</tasks>

<success_criteria>
- Both renderers expose current-view export handles
- Capture can force a fresh frame without breaking renderer continuity
- GraphWorkspace can provide the active renderer export target
</success_criteria>

<output>
After completion, create `.planning/phases/07-export/07-01-SUMMARY.md`.
</output>
