---
phase: 07-export
plan: 02
type: execute
wave: 2
depends_on:
  - "07-01"
files_modified:
  - package.json
  - src/lib/export/reportExporter.ts
  - src/lib/export/exportTypes.ts
  - src/lib/export/__tests__/reportExporter.test.ts
autonomous: true
requirements:
  - EXPORT-01
  - EXPORT-02
  - EXPORT-03

requirements_addressed:
  - EXPORT-01
  - EXPORT-02
  - EXPORT-03

must_haves:
  truths:
    - "PNG export uses the active renderer's current canvas view"
    - "PDF export includes detailed case context beyond the graph snapshot"
    - "Both direct-canvas and html2canvas-related paths are represented or validated in the export utility layer"
  artifacts:
    - path: "src/lib/export/reportExporter.ts"
      provides: "PNG and PDF export orchestration"
      contains: "exportPNG"
    - path: "src/lib/export/reportExporter.ts"
      provides: "Detailed PDF report generation"
      contains: "exportPDF"
---

<objective>
Create the export utility layer for PNG and PDF outputs.

Purpose: Export logic should stay outside UI components and handle both direct-canvas capture and detailed PDF composition in one reusable module.
Output: export utilities, types, and focused utility tests.
</objective>

<context>
@.planning/phases/07-export/07-CONTEXT.md
@.planning/phases/07-export/07-RESEARCH.md
@src/store/caseStore.ts
@src/components/pages/CaseWorkspacePage.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add export dependencies and shared export types</name>
  <files>package.json, src/lib/export/exportTypes.ts</files>
  <action>
Add the libraries and shared type contracts needed for direct PNG export, detailed PDF report composition, and any validated html2canvas support path.
  </action>
  <verify>
    <automated>Export utility dependencies and types compile cleanly</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 2: Implement direct PNG export and detailed PDF generation</name>
  <files>src/lib/export/reportExporter.ts</files>
  <action>
Build utility functions that export the active renderer's current canvas view as PNG and generate a detailed PDF report with metadata, entity list, connection summary, and current evidence/highlight context.
  </action>
  <verify>
    <automated>Export utilities expose PNG and PDF paths with current-view-first behavior</automated>
  </verify>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add focused utility tests for export composition</name>
  <files>src/lib/export/__tests__/reportExporter.test.ts</files>
  <action>
Add focused tests around export payload assembly, filename/report metadata, and PDF content composition assumptions so the export layer is guarded before UI wiring lands.
  </action>
  <verify>
    <automated>Utility tests cover PNG/PDF export orchestration and detailed report content assembly</automated>
  </verify>
</task>

</tasks>

<success_criteria>
- Export utility layer supports PNG and detailed PDF generation
- Active-renderer current-view capture remains the PNG source of truth
- Export utility behavior is covered by focused tests
</success_criteria>

<output>
After completion, create `.planning/phases/07-export/07-02-SUMMARY.md`.
</output>
