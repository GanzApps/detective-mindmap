---
phase: 07-export
plan: 04
type: execute
wave: 4
depends_on:
  - "07-01"
  - "07-02"
  - "07-03"
files_modified:
  - src/components/layout/__tests__/CaseHeader.test.tsx
  - src/components/pages/__tests__/CaseWorkspacePage.test.tsx
  - src/components/graph/__tests__/GraphWorkspace.test.tsx
  - .planning/phases/07-export/*
autonomous: true
requirements:
  - EXPORT-01
  - EXPORT-02
  - EXPORT-03
  - ARCH-05

requirements_addressed:
  - EXPORT-01
  - EXPORT-02
  - EXPORT-03
  - ARCH-05

must_haves:
  truths:
    - "Export UI and orchestration are protected by focused regression tests"
    - "Verification proves current-view PNG and detailed PDF flows work end to end"
    - "Phase 7 completion is documented with summaries and verification artifacts"
  artifacts:
    - path: "src/components/layout/__tests__/CaseHeader.test.tsx"
      provides: "Export menu coverage"
      contains: "Export Report"
    - path: ".planning/phases/07-export/07-VERIFICATION.md"
      provides: "Phase completion evidence"
      contains: "Verification Report"
---

<objective>
Close Phase 7 with tests and verification.

Purpose: Export is only trustworthy if the UI flow, active-renderer routing, and report paths are covered by tests and verified together.
Output: focused regression coverage plus full phase verification artifacts.
</objective>

<context>
@.planning/phases/07-export/07-CONTEXT.md
@.planning/phases/07-export/07-RESEARCH.md
@src/components/layout/__tests__/CaseHeader.test.tsx
@src/components/graph/__tests__/GraphWorkspace.test.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add focused tests for export UI and orchestration</name>
  <files>src/components/layout/__tests__/CaseHeader.test.tsx, src/components/pages/__tests__/CaseWorkspacePage.test.tsx, src/components/graph/__tests__/GraphWorkspace.test.tsx</files>
  <action>
Add focused regression coverage for the export menu, active renderer export wiring, and page-level orchestration so the user-facing flow is protected.
  </action>
  <verify>
    <automated>Tests assert export menu behavior and active-renderer export routing</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 2: Run full verification for Phase 7 export behavior</name>
  <files>.planning/phases/07-export/*</files>
  <action>
Run the full verification loop after implementation and document that current-view PNG export, detailed PDF export, and menu-based export UX all work together.
  </action>
  <verify>
    <automated>`tsc`, `jest`, and `next build` all pass for the completed Phase 7 implementation</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 3: Write summaries and phase completion evidence</name>
  <files>.planning/phases/07-export/*</files>
  <action>
Create the Phase 7 summaries and verification artifact so the final export phase has the same completion trail as the earlier phases.
  </action>
  <verify>
    <automated>Phase 7 includes plan summaries and a final verification report</automated>
  </verify>
</task>

</tasks>

<success_criteria>
- Export UI and orchestration are covered by focused tests
- Full verification proves PNG/PDF flows work together
- Phase 7 completion artifacts are ready for milestone completion
</success_criteria>

<output>
After completion, create `.planning/phases/07-export/07-04-SUMMARY.md`.
</output>
