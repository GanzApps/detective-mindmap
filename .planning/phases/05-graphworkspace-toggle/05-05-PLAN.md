---
phase: 05-graphworkspace-toggle
plan: 05
type: execute
wave: 4
depends_on:
  - "05-04"
files_modified:
  - src/__tests__/app-shell.test.tsx
  - src/components/layout/__tests__/CaseHeader.test.tsx
  - src/components/layout/__tests__/EvidenceSidebar.test.tsx
  - src/components/graph/__tests__/NodeDetailPanel.test.tsx
autonomous: true
requirements:
  - ARCH-05
  - WS-04

must_haves:
  truths:
    - "Component-level coverage exists for `CaseHeader`, `EvidenceSidebar`, and `NodeDetailPanel`"
    - "The shared workspace integration has a smoke guard for mounted dual-renderer behavior"
    - "Panel dismissal behavior is protected by tests"
  artifacts:
    - path: "src/components/graph/__tests__/NodeDetailPanel.test.tsx"
      provides: "Focused dismissal and rendering coverage for the shared detail surface"
      contains: "NodeDetailPanel"
    - path: "src/__tests__/app-shell.test.tsx"
      provides: "Shell/workspace smoke coverage for the new shared graph area"
      contains: "GraphWorkspace"
---

<objective>
Close Phase 5 with component-level regression coverage.

Purpose: The new shared workspace and panel behavior must be protected with tests at the component layer rather than relying only on shell smoke checks or canvas interactions.
Output: focused component coverage for the shell pieces required by Phase 5.
</objective>

<context>
@.planning/phases/05-graphworkspace-toggle/05-CONTEXT.md
@src/__tests__/app-shell.test.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add focused tests for `CaseHeader` and `EvidenceSidebar`</name>
  <files>src/components/layout/__tests__/CaseHeader.test.tsx, src/components/layout/__tests__/EvidenceSidebar.test.tsx</files>
  <action>
Add component-level coverage for the shell controls and evidence navigation surface required by the roadmap, using focused render tests rather than broad end-to-end simulation.
  </action>
  <verify>
    <automated>Tests assert the expected controls, labels, and category/file rendering for both layout components</automated>
  </verify>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add focused tests for `NodeDetailPanel` rendering and dismissal</name>
  <files>src/components/graph/__tests__/NodeDetailPanel.test.tsx</files>
  <action>
Cover the shared panel’s selected-node rendering, property output, and Escape/deselect dismissal behavior.
  </action>
  <verify>
    <automated>Tests assert panel content and dismissal behavior for the shared detail surface</automated>
  </verify>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Update workspace shell smoke coverage for the new shared graph area</name>
  <files>src/__tests__/app-shell.test.tsx</files>
  <action>
Refresh shell smoke coverage so it asserts the presence of the new shared `GraphWorkspace` and panel integration rather than the previous renderer-branching shell layout.
  </action>
  <verify>
    <automated>Jest passes with updated shell smoke expectations around the shared graph workspace</automated>
  </verify>
</task>

</tasks>

<success_criteria>
- Focused component coverage exists for `CaseHeader`, `EvidenceSidebar`, and `NodeDetailPanel`
- The shared workspace shell has updated smoke coverage
- Panel dismissal behavior is protected by tests
</success_criteria>

<output>
After completion, create `.planning/phases/05-graphworkspace-toggle/05-05-SUMMARY.md`.
</output>
