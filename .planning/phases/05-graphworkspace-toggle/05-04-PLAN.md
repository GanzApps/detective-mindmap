---
phase: 05-graphworkspace-toggle
plan: 04
type: execute
wave: 3
depends_on:
  - "05-02"
  - "05-03"
files_modified:
  - src/components/graph/GraphWorkspace.tsx
  - src/components/layout/CaseWorkspaceShell.tsx
  - src/lib/data/evidenceHighlights.ts
autonomous: true
requirements:
  - WS-01
  - WS-03

must_haves:
  truths:
    - "Evidence-driven highlights flow through the shared workspace into whichever renderer is active"
    - "The shared detail surface and selection behavior align with evidence highlight flows rather than conflicting with them"
    - "The workspace shell hands graph-area concerns to `GraphWorkspace` cleanly"
  artifacts:
    - path: "src/components/graph/GraphWorkspace.tsx"
      provides: "Evidence highlight integration across both renderers"
      contains: "highlightedNodeIds"
    - path: "src/components/layout/CaseWorkspaceShell.tsx"
      provides: "Shell-to-workspace coordination"
      contains: "GraphWorkspace"
---

<objective>
Finish the shared workspace behavior around evidence highlights and shell coordination.

Purpose: Phase 5 should leave the graph area feeling unified, not like two renderer islands glued into the shell. Evidence highlights and shared detail behavior need to flow through the new workspace cleanly.
Output: completed shared workspace coordination for renderer selection and evidence-driven highlight behavior.
</objective>

<context>
@.planning/phases/05-graphworkspace-toggle/05-CONTEXT.md
@src/lib/data/evidenceHighlights.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Route evidence highlights through the shared workspace</name>
  <files>src/components/graph/GraphWorkspace.tsx, src/components/layout/CaseWorkspaceShell.tsx</files>
  <action>
Ensure the evidence sidebar highlight flow reaches both renderers through the shared workspace wrapper so mode changes do not break highlighted-node continuity.
  </action>
  <verify>
    <automated>Highlighted node IDs are passed through `GraphWorkspace` into both renderer surfaces</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 2: Align shared detail behavior with evidence-based selection states</name>
  <files>src/components/graph/GraphWorkspace.tsx</files>
  <action>
Make sure the shared panel and selection logic behave coherently when evidence clicks drive highlighted nodes and an active node is also selected.
  </action>
  <verify>
    <automated>The panel and highlight logic can coexist without duplicating conflicting state</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 3: Simplify shell-to-graph-area responsibilities</name>
  <files>src/components/layout/CaseWorkspaceShell.tsx</files>
  <action>
Move graph-area concerns into the new workspace wrapper so the shell remains focused on higher-level layout, CRUD, and evidence-sidebar concerns.
  </action>
  <verify>
    <automated>`CaseWorkspaceShell` no longer contains duplicated graph-detail logic that belongs in `GraphWorkspace`</automated>
  </verify>
</task>

</tasks>

<success_criteria>
- Evidence-driven highlights flow through the shared workspace to both renderers
- Shared detail behavior remains coherent with highlight and selection state
- `CaseWorkspaceShell` cleanly delegates graph-area behavior to `GraphWorkspace`
</success_criteria>

<output>
After completion, create `.planning/phases/05-graphworkspace-toggle/05-04-SUMMARY.md`.
</output>
