---
phase: 05-graphworkspace-toggle
plan: 02
type: execute
wave: 2
depends_on:
  - "05-01"
files_modified:
  - src/components/graph/GraphWorkspace.tsx
  - src/components/graph/ForceGraph2D.tsx
  - src/components/graph/MindMap3D.tsx
  - src/store/caseStore.ts
autonomous: true
requirements:
  - WS-01
  - WS-02

must_haves:
  truths:
    - "Selected-node state is consumed identically by both renderers through the shared workspace"
    - "Renderer-local detail behavior is removed or unified so the panel source of truth is not duplicated"
    - "Toggling between modes preserves visible node highlighting and renderer-internal state"
  artifacts:
    - path: "src/components/graph/GraphWorkspace.tsx"
      provides: "Shared selection and renderer continuity wiring"
      contains: "selectedNodeId"
    - path: "src/components/graph/MindMap3D.tsx"
      provides: "Renderer integration with shared detail ownership"
      contains: "onSelectNode"
---

<objective>
Unify shared selection and renderer continuity under the new workspace wrapper.

Purpose: Phase 5 must prove that selection and highlights are not merely parallel features in each renderer, but a genuinely shared state that survives mode changes without resetting the renderer internals.
Output: shared selection flow with renderer-local inspector duplication removed.
</objective>

<context>
@.planning/phases/05-graphworkspace-toggle/05-CONTEXT.md
@src/store/caseStore.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Route both renderers through one shared selected-node contract</name>
  <files>src/components/graph/GraphWorkspace.tsx, src/store/caseStore.ts</files>
  <action>
Ensure the shared selected-node and highlight state flows cleanly from the store into both renderers and back out through one callback path inside the workspace wrapper.
  </action>
  <verify>
    <automated>Both renderers receive the same `selectedNodeId` and `onSelectNode` contract from `GraphWorkspace`</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 2: Remove competing renderer-local detail ownership</name>
  <files>src/components/graph/MindMap3D.tsx, src/components/graph/ForceGraph2D.tsx</files>
  <action>
Strip or simplify renderer-local inspector content that conflicts with the new shared detail surface so there is one authoritative node-detail experience in the workspace.
  </action>
  <verify>
    <automated>Renderer components no longer own the primary node-detail panel experience</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 3: Preserve highlight continuity through mode switches</name>
  <files>src/components/graph/GraphWorkspace.tsx</files>
  <action>
Confirm that toggling mode keeps the active node highlighted and does not reset the internal simulation/camera state of the hidden renderer.
  </action>
  <verify>
    <automated>The workspace toggle changes visibility only and preserves the current selected/highlighted node state</automated>
  </verify>
</task>

</tasks>

<success_criteria>
- Both renderers consume and emit one shared selected-node contract
- The workspace has one authoritative detail-surface owner
- Mode changes preserve visible highlighting and renderer continuity
</success_criteria>

<output>
After completion, create `.planning/phases/05-graphworkspace-toggle/05-02-SUMMARY.md`.
</output>
