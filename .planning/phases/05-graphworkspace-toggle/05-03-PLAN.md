---
phase: 05-graphworkspace-toggle
plan: 03
type: execute
wave: 2
depends_on:
  - "05-01"
  - "05-02"
files_modified:
  - src/components/graph/NodeDetailPanel.tsx
  - src/components/graph/GraphWorkspace.tsx
autonomous: true
requirements:
  - WS-03
  - WS-04

must_haves:
  truths:
    - "A shared floating `NodeDetailPanel` exists near the graph area rather than as a permanent layout rail"
    - "The panel feels like a richer case-analysis card with stronger visual hierarchy"
    - "The panel dismisses on deselect and Escape"
  artifacts:
    - path: "src/components/graph/NodeDetailPanel.tsx"
      provides: "Shared floating node-detail surface"
      contains: "NodeDetailPanel"
    - path: "src/components/graph/GraphWorkspace.tsx"
      provides: "Workspace-level panel placement and dismissal wiring"
      contains: "NodeDetailPanel"
---

<objective>
Create the shared floating node-detail surface.

Purpose: Phase 5 needs a common investigation card that feels richer than the current renderer-local summaries while staying visually attached to the graph workspace.
Output: shared floating `NodeDetailPanel` with dismissal behavior and richer information hierarchy.
</objective>

<context>
@.planning/phases/05-graphworkspace-toggle/05-CONTEXT.md
@image.png
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build `NodeDetailPanel` as a shared floating inspector card</name>
  <files>src/components/graph/NodeDetailPanel.tsx</files>
  <action>
Create a reusable node-detail component that presents the selected node label, type, relationship summary, and raw properties with stronger visual hierarchy than the renderer-local summaries.
  </action>
  <verify>
    <automated>The panel renders selected node metadata and properties from shared inputs</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 2: Place the panel near the graph area instead of in a permanent side column</name>
  <files>src/components/graph/GraphWorkspace.tsx</files>
  <action>
Mount the panel as a floating workspace-attached surface so it visually belongs to the active graph area instead of consuming a fixed layout column.
  </action>
  <verify>
    <automated>The panel is rendered as a floating overlay/inspector within the graph workspace region</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 3: Wire dismiss behavior for deselect and Escape</name>
  <files>src/components/graph/GraphWorkspace.tsx, src/components/graph/NodeDetailPanel.tsx</files>
  <action>
Dismiss the panel when the selection clears, when empty graph space is clicked, and when the Escape key is pressed.
  </action>
  <verify>
    <automated>The panel responds to shared deselect flow and Escape-key dismissal</automated>
  </verify>
</task>

</tasks>

<success_criteria>
- A shared floating `NodeDetailPanel` exists
- The panel feels richer than the old renderer-local summaries
- Deselect and Escape close the panel cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/05-graphworkspace-toggle/05-03-SUMMARY.md`.
</output>
