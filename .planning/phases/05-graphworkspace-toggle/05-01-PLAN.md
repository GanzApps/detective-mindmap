---
phase: 05-graphworkspace-toggle
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/graph/GraphWorkspace.tsx
  - src/components/layout/CaseWorkspaceShell.tsx
autonomous: true
requirements:
  - WS-01
  - WS-02

must_haves:
  truths:
    - "A dedicated `GraphWorkspace` owns both mounted renderers and the visible-mode toggle surface"
    - "Both renderers stay mounted and are hidden with CSS instead of being conditionally unmounted"
    - "The shell delegates graph-area composition to the new workspace wrapper"
  artifacts:
    - path: "src/components/graph/GraphWorkspace.tsx"
      provides: "Shared dual-renderer workspace wrapper"
      contains: "GraphWorkspace"
    - path: "src/components/layout/CaseWorkspaceShell.tsx"
      provides: "Shell integration path for the shared workspace"
      contains: "GraphWorkspace"
---

<objective>
Create the shared dual-renderer workspace shell.

Purpose: Phase 5 needs a single graph-area owner that can mount both renderers, preserve their internal state, and toggle visibility without unmounting either surface.
Output: a reusable `GraphWorkspace` component wired into the existing case workspace shell.
</objective>

<context>
@.planning/phases/05-graphworkspace-toggle/05-CONTEXT.md
@src/components/layout/CaseWorkspaceShell.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build `GraphWorkspace` as the graph-area owner</name>
  <files>src/components/graph/GraphWorkspace.tsx</files>
  <action>
Create a shared graph-area wrapper that accepts graph data, current mode, selected node state, highlight state, and callbacks, and renders both `ForceGraph2D` and `MindMap3D` inside the same workspace boundary.
  </action>
  <verify>
    <automated>`GraphWorkspace` renders both renderer surfaces from shared props</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 2: Keep both renderers mounted with CSS-only visibility changes</name>
  <files>src/components/graph/GraphWorkspace.tsx</files>
  <action>
Use wrapper elements with `display:none`/`display:block` style switching so the inactive renderer stays mounted and preserves its internal camera or simulation state across toggles.
  </action>
  <verify>
    <automated>The workspace uses CSS visibility toggling rather than conditional renderer mounting</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 3: Replace graph-area branching in the shell with `GraphWorkspace`</name>
  <files>src/components/layout/CaseWorkspaceShell.tsx</files>
  <action>
Remove direct 2D/3D branching from the shell and delegate renderer composition to the new shared workspace component.
  </action>
  <verify>
    <automated>The shell now renders `GraphWorkspace` instead of directly switching between renderer components</automated>
  </verify>
</task>

</tasks>

<success_criteria>
- `GraphWorkspace` exists as the shared graph-area owner
- Both renderers remain mounted and toggle through CSS visibility only
- The case workspace shell delegates graph rendering to the shared wrapper
</success_criteria>

<output>
After completion, create `.planning/phases/05-graphworkspace-toggle/05-01-SUMMARY.md`.
</output>
