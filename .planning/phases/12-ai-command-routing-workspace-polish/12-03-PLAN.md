# Phase 12-03 Plan: Quick Chips, History, and Final Polish

## Goal

Finish the command surface with useful quick actions, lightweight history, and final shell cohesion, then validate the milestone end-state.

## Tasks

1. Add a mixed chip system: static baseline chips plus context-aware chips.
2. Add lightweight recent command history to the command surface.
3. Tune spacing, hierarchy, and feedback so the command band feels fully integrated.
4. Add/update tests and run full validation.

## Files

- `src/components/layout/AICommandBar.tsx`
- `src/components/layout/CaseWorkspaceShell.tsx`
- tests as needed

## Done When

- quick chips feel useful in idle and contextual states
- recent command history is readable but low-noise
- the command surface feels like part of the workspace rather than an add-on
