# Phase 12-02 Plan: Right-Panel AI Result Mode

## Goal

Route command results into the existing right-side panel through a clean `AI result` mode that stays distinct from node detail.

## Tasks

1. Add right-panel mode handling for node detail vs AI result.
2. Define the result payload structure for known-intent commands.
3. Render AI result summaries, findings, and next actions in the panel.
4. Keep transitions between node detail and AI result understandable and reversible.

## Files

- `src/components/layout/WorkspaceAnalysisPanel.tsx`
- `src/components/layout/CaseWorkspaceShell.tsx`
- `src/components/graph/GraphWorkspace.tsx`
- state/helpers as needed

## Done When

- AI results appear in a dedicated panel mode
- node detail and AI result do not visually clash
