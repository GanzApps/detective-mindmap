# Phase 12-01 Plan: Command Surface + Known-Intent Foundation

## Goal

Build the shared command entry behavior so typed prompts and slash-style commands feed the same known-intent parser and execution pipeline.

## Tasks

1. Audit the existing command bar component and command/state wiring.
2. Define the initial known-intent set and mapping structure.
3. Support natural-language and slash-style entry in one parser path.
4. Expose inline command execution state in the command surface.

## Files

- `src/components/layout/AICommandBar.tsx`
- `src/store/caseStore.ts`
- command parsing helpers as needed

## Done When

- typed prompts and slash commands use the same known-intent execution path
- inline execution state is visible in the command surface
