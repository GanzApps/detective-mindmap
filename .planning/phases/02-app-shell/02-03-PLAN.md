---
phase: 02-app-shell
plan: 03
type: execute
wave: 3
depends_on:
  - "02-02"
files_modified:
  - src/components/layout/CaseHeader.tsx
  - src/components/layout/EvidenceSidebar.tsx
  - src/components/layout/CaseWorkspaceShell.tsx
  - src/app/cases/[caseId]/page.tsx
autonomous: true
requirements:
  - SHELL-01
  - SHELL-02
  - SHELL-03
  - SHELL-04

must_haves:
  truths:
    - "The workspace route shows a case header with case name, status badge, Export Report button, and Actions dropdown"
    - "The workspace route shows an evidence sidebar grouped by category and file"
    - "Clicking an evidence file updates shared highlight state for later graph integration"
    - "Entity and connection counts are visible in the shell toolbar area"
  artifacts:
    - path: "src/components/layout/CaseHeader.tsx"
      provides: "Case header component"
      contains: "Export Report"
    - path: "src/components/layout/EvidenceSidebar.tsx"
      provides: "Evidence category/file tree"
      contains: "onEvidenceSelect"
  key_links:
    - from: "src/components/layout/EvidenceSidebar.tsx"
      to: "src/store/caseStore.ts"
      via: "highlight callback"
      pattern: "setHighlighted"
---

<objective>
Implement the Phase 2 workspace shell components.

Purpose: This plan turns the route shell into an actual investigation workspace frame with the correct header, sidebar, and count displays.
Output: CaseHeader, EvidenceSidebar, and workspace shell composition with highlight callbacks wired into shared state.
</objective>

<context>
@.planning/phases/02-app-shell/02-CONTEXT.md
@image.png
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build the CaseHeader component</name>
  <files>src/components/layout/CaseHeader.tsx</files>
  <action>
Create the header with case name, status badge, Export Report button, Actions dropdown, and a compact toolbar area for entity/connection counts. Keep the shell functional-first, not fully polished.
  </action>
  <verify>
    <automated>Header renders all SHELL-01 controls and the count display</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 2: Build the EvidenceSidebar component</name>
  <files>src/components/layout/EvidenceSidebar.tsx</files>
  <action>
Render evidence categories and their files in a sidebar that already feels investigative rather than generic. File click should call a typed callback carrying the selected evidence item and associated highlight intent.
  </action>
  <verify>
    <automated>Sidebar renders category sections and file rows from case evidence data</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 3: Compose the workspace shell and wire evidence click state</name>
  <files>src/components/layout/CaseWorkspaceShell.tsx, src/app/cases/[caseId]/page.tsx</files>
  <action>
Add a layout wrapper that combines the header, sidebar, toolbar/count region, and graph placeholder. Wire evidence file clicks into store state for highlighted entities even though renderer consumption is deferred to Phase 5.
  </action>
  <verify>
    <automated>Workspace page composes CaseHeader and EvidenceSidebar and updates shared state on evidence click</automated>
  </verify>
</task>

</tasks>

<success_criteria>
- Workspace route shows the correct header controls
- Evidence sidebar is grouped and clickable
- Toolbar counts are visible and derived from graph data
- Evidence file clicks update highlight state for later graph use
</success_criteria>

<output>
After completion, create `.planning/phases/02-app-shell/02-03-SUMMARY.md`.
</output>
