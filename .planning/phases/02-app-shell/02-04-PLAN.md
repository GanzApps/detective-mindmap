---
phase: 02-app-shell
plan: 04
type: execute
wave: 3
depends_on:
  - "02-02"
files_modified:
  - src/components/crud/CaseModal.tsx
  - src/components/crud/EntityModal.tsx
  - src/components/crud/ConnectionModal.tsx
  - src/components/crud/DeleteConfirmDialog.tsx
  - src/components/layout/CaseWorkspaceShell.tsx
  - src/app/cases/page.tsx
autonomous: true
requirements:
  - CASE-01
  - CASE-02
  - CASE-03
  - CASE-04
  - CASE-05

must_haves:
  truths:
    - "Case creation is available from a modal/dialog flow"
    - "Entity creation and connection creation are available from modal/dialog flows in the workspace"
    - "Entity and connection deletion are available through explicit confirmation flows"
    - "All modal actions mutate the Zustand store and reflect immediately in the UI"
  artifacts:
    - path: "src/components/crud/CaseModal.tsx"
      provides: "Case creation modal"
      contains: "onSubmit"
    - path: "src/components/crud/EntityModal.tsx"
      provides: "Entity creation modal"
      contains: "type"
    - path: "src/components/crud/ConnectionModal.tsx"
      provides: "Connection creation modal"
      contains: "source"
  key_links:
    - from: "src/components/crud/*.tsx"
      to: "src/store/caseStore.ts"
      via: "store actions"
      pattern: "createCase|addEntity|addConnection|deleteEntity|deleteConnection"
---

<objective>
Add the modal-driven CRUD flows for Phase 2.

Purpose: The user chose explicit dialog flows over inline editing, and Phase 2 must already support mutating cases, entities, and connections inside the shell.
Output: Modal/dialog components and integration points that mutate the shared store and update the visible shell immediately.
</objective>

<context>
@.planning/phases/02-app-shell/02-CONTEXT.md
@.planning/REQUIREMENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build modal/dialog components for case, entity, and connection creation</name>
  <files>src/components/crud/CaseModal.tsx, src/components/crud/EntityModal.tsx, src/components/crud/ConnectionModal.tsx</files>
  <action>
Implement modal-driven create flows with typed forms and minimal validation. Keep the first version functional and legible rather than visually rich.
  </action>
  <verify>
    <automated>Modal components expose submit handlers with typed payloads</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 2: Build explicit delete confirmation dialog flows</name>
  <files>src/components/crud/DeleteConfirmDialog.tsx</files>
  <action>
Add a reusable delete confirmation dialog used for entity and connection deletion so destructive actions remain clear and deliberate in Phase 2.
  </action>
  <verify>
    <automated>Delete flow requires explicit confirmation before store mutation</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 3: Integrate CRUD dialogs into the list and workspace shells</name>
  <files>src/components/layout/CaseWorkspaceShell.tsx, src/app/cases/page.tsx</files>
  <action>
Wire the create/delete dialogs into the visible UI so mutations update the list page and workspace route immediately from shared state.
  </action>
  <verify>
    <automated>Creating or deleting data changes rendered UI without reload</automated>
  </verify>
</task>

</tasks>

<success_criteria>
- Case, entity, and connection creation use modal/dialog flows
- Entity and connection deletion use explicit confirmation
- CRUD actions go through the store and update visible UI immediately
</success_criteria>

<output>
After completion, create `.planning/phases/02-app-shell/02-04-SUMMARY.md`.
</output>
