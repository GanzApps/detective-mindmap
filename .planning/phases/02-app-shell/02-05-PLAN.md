---
phase: 02-app-shell
plan: 05
type: execute
wave: 4
depends_on:
  - "02-03"
  - "02-04"
files_modified:
  - src/app/cases/page.tsx
  - src/app/cases/[caseId]/page.tsx
  - src/__tests__/app-shell.test.tsx
  - package.json
autonomous: true
requirements:
  - CASE-06
  - SHELL-04
  - ARCH-02
  - ARCH-03

must_haves:
  truths:
    - "The /cases list shows case name, entity count, and last modified date"
    - "A create case -> add entity -> view list smoke flow works through shared state"
    - "Phase 2 routes build successfully with the no-SSR graph pattern intact"
    - "App shell behavior has at least one automated route/component smoke test"
  artifacts:
    - path: "src/app/cases/page.tsx"
      provides: "Rendered case list with metrics"
      contains: "updatedAt"
    - path: "src/__tests__/app-shell.test.tsx"
      provides: "App shell smoke test coverage"
      contains: "cases"
  key_links:
    - from: "src/app/cases/page.tsx"
      to: "src/store/caseStore.ts"
      via: "rendered case metrics"
      pattern: "graph.nodes.length|graph.edges.length"
---

<objective>
Finish and prove the Phase 2 shell.

Purpose: This final plan closes the loop by rendering the dashboard-style case list with real metrics, verifying the state-driven shell flow, and adding automated smoke coverage for the App Shell phase.
Output: Complete list/workspace shell flow plus smoke verification artifacts.
</objective>

<context>
@.planning/phases/02-app-shell/02-CONTEXT.md
@.planning/ROADMAP.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Finish the dashboard-style case list rendering</name>
  <files>src/app/cases/page.tsx</files>
  <action>
Render each case with name, status, entity count, connection count if useful, and last modified date in a dashboard-style presentation. The route should feel like the beginning of the product, not an admin grid.
  </action>
  <verify>
    <automated>Case list renders case name, entity count, and updatedAt-derived date</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 2: Validate the create -> mutate -> reflect shell flow</name>
  <files>src/app/cases/page.tsx, src/app/cases/[caseId]/page.tsx</files>
  <action>
Ensure that creating a case and mutating its entities/connections through the shell reflects in the visible routes without reload. Keep the flow store-driven and deterministic.
  </action>
  <verify>
    <automated>Manual smoke path defined and executable: create case -> add entity -> list reflects counts</automated>
  </verify>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add automated smoke coverage for the app shell</name>
  <files>src/__tests__/app-shell.test.tsx, package.json</files>
  <action>
Add at least one React Testing Library or equivalent smoke test covering the list shell and/or workspace shell composition, plus any test runner wiring needed for component-safe execution in this phase.
  </action>
  <verify>
    <automated>Build and test commands cover the new app shell smoke test without SSR issues</automated>
  </verify>
</task>

</tasks>

<success_criteria>
- `/cases` renders dashboard-style case cards/items with required metadata
- State-driven shell changes are visible without reload
- Build still succeeds with the client-only graph placeholder pattern
- Automated smoke coverage exists for the App Shell phase
</success_criteria>

<output>
After completion, create `.planning/phases/02-app-shell/02-05-SUMMARY.md`.
</output>
