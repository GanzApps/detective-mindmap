---
phase: 02-app-shell
plan: 02
type: execute
wave: 2
depends_on:
  - "02-01"
files_modified:
  - src/store/caseStore.ts
  - src/hooks/useCaseData.ts
  - src/lib/data/caseRepository.ts
autonomous: true
requirements:
  - ARCH-03
  - CASE-01

must_haves:
  truths:
    - "A Zustand store owns case data, selected node ID, active filters, and view mode"
    - "The store exposes actions to create cases, add entities, add connections, and delete entities/connections"
    - "useCaseData loads a case from caseRepository rather than importing mockCases directly"
  artifacts:
    - path: "src/store/caseStore.ts"
      provides: "Global case state store"
      contains: "create("
    - path: "src/hooks/useCaseData.ts"
      provides: "Repository-backed case loading hook"
      contains: "mockCaseRepository"
  key_links:
    - from: "src/hooks/useCaseData.ts"
      to: "src/lib/data/caseRepository.ts"
      via: "repository call"
      pattern: "listCases|fetchCase"
---

<objective>
Create the Phase 2 state layer and repository-backed loading hooks.

Purpose: All Phase 2 UI work depends on a central store for case data and interactions, and the UI must continue honoring the repository abstraction from Phase 1.
Output: Zustand store slices, CRUD actions, and loading hooks wired to caseRepository.
</objective>

<context>
@.planning/phases/02-app-shell/02-CONTEXT.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/research/ARCHITECTURE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the Zustand case store shape and selectors</name>
  <files>src/store/caseStore.ts</files>
  <action>
Define state for case collection, active case, selected node ID, active filters, highlighted evidence-linked entity IDs, and view mode (`2d` | `3d`). Keep the shape ready for later graph phases without pulling graph implementation into the store.
  </action>
  <verify>
    <automated>Store exports state, selectors, and typed actions without `any`</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 2: Implement CRUD actions on the store</name>
  <files>src/store/caseStore.ts</files>
  <action>
Add actions for creating a case, adding/removing entities, adding/removing connections, and setting evidence-highlight callbacks. Ensure deleting an entity removes its connected edges.
  </action>
  <verify>
    <automated>Action signatures cover CASE-01 through CASE-05 requirements relevant to state</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 3: Create useCaseData hooks backed by caseRepository</name>
  <files>src/hooks/useCaseData.ts</files>
  <action>
Add hooks for loading the case list and an individual case via `caseRepository`. Hydrate the Zustand store from the hook instead of importing mock fixtures in route components.
  </action>
  <verify>
    <automated>Hook imports from caseRepository only, not mockCases directly</automated>
  </verify>
</task>

</tasks>

<success_criteria>
- Zustand store exists with global case state and view mode
- CRUD-capable store actions exist for cases, entities, and connections
- Repository-backed hooks exist for list and case loading
- Phase 2 UI can depend on the store without direct fixture imports
</success_criteria>

<output>
After completion, create `.planning/phases/02-app-shell/02-02-SUMMARY.md`.
</output>
