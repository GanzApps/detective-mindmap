---
phase: 02-app-shell
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/page.tsx
  - src/app/cases/page.tsx
  - src/app/cases/[caseId]/page.tsx
  - src/app/cases/loading.tsx
  - src/app/cases/[caseId]/loading.tsx
autonomous: true
requirements:
  - CASE-06
  - ARCH-02

must_haves:
  truths:
    - "Visiting / redirects directly to /cases"
    - "Visiting /cases renders a dashboard-style case list shell without SSR crashes"
    - "Visiting /cases/[caseId] renders a functional workspace shell route without SSR crashes"
  artifacts:
    - path: "src/app/page.tsx"
      provides: "Root redirect into /cases"
      contains: "redirect('/cases')"
    - path: "src/app/cases/page.tsx"
      provides: "Case list route"
      contains: "CaseListPage"
    - path: "src/app/cases/[caseId]/page.tsx"
      provides: "Workspace route shell"
      contains: "dynamic("
  key_links:
    - from: "src/app/page.tsx"
      to: "src/app/cases/page.tsx"
      via: "Next.js redirect"
      pattern: "redirect('/cases')"
---

<objective>
Establish the navigable App Router shell for Phase 2.

Purpose: The application needs stable routes before any Zustand state, header/sidebar composition, or CRUD workflow can be wired.
Output: Root redirect, dashboard-style case list route, and case workspace shell route with loading states and the no-SSR graph placeholder pattern in place.
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-app-shell/02-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace the root landing page with a redirect to /cases</name>
  <files>src/app/page.tsx</files>
  <action>
Use App Router's `redirect` helper so `/` immediately enters the case list flow. Remove the temporary Phase 1 placeholder page.
  </action>
  <verify>
    <automated>Root route source contains redirect('/cases')</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 2: Create the /cases list route and /cases/[caseId] workspace route shells</name>
  <files>src/app/cases/page.tsx, src/app/cases/[caseId]/page.tsx, src/app/cases/loading.tsx, src/app/cases/[caseId]/loading.tsx</files>
  <action>
Build the initial route files and loading states. The list page should be a dashboard-style shell, not a plain admin table. The workspace page should establish the high-level layout zones for header, sidebar, toolbar, and graph area.
  </action>
  <verify>
    <automated>Route files exist and export valid page/loading components</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 3: Establish the dynamic({ ssr: false }) graph placeholder pattern on the workspace page</name>
  <files>src/app/cases/[caseId]/page.tsx</files>
  <action>
Create a client-only placeholder component import for the future graph region using `next/dynamic`. This placeholder should render a stable shell box and prove the SSR-safe pattern before D3 or Canvas code exists.
  </action>
  <verify>
    <automated>`next build` completes without SSR/runtime errors from the workspace route</automated>
  </verify>
</task>

</tasks>

<success_criteria>
- `/` redirects to `/cases`
- `/cases` exists as a dashboard-style list shell
- `/cases/[caseId]` exists as a functional workspace shell
- Graph placeholder is wrapped with `dynamic({ ssr: false })`
- Loading states exist for both routes
</success_criteria>

<output>
After completion, create `.planning/phases/02-app-shell/02-01-SUMMARY.md`.
</output>
