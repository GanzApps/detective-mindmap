---
phase: 09-workspace-frame-panels
plan: 03
type: execute
wave: 3
depends_on:
  - 09-01-PLAN.md
  - 09-02-PLAN.md
files_modified:
  - src/components/layout/CaseWorkspaceShell.tsx
  - src/components/layout/TimelineBar.tsx
  - src/components/layout/AICommandBar.tsx
  - src/__tests__/app-shell.test.tsx
  - src/components/layout/__tests__/WorkspacePanels.test.tsx
autonomous: true
requirements:
  - SHELL-08
  - WS-11

requirements_addressed:
  - SHELL-08
  - WS-11

must_haves:
  truths:
    - "Timeline remains visible as a first-class surface but is collapsed by default"
    - "Workspace shell bands feel coherent after the left rail, right panel, and minimap changes"
    - "Shell/panel regressions are covered by tests and full validation passes"
  artifacts:
    - path: "src/components/layout/TimelineBar.tsx"
      provides: "collapsed-by-default timeline behavior"
      contains: "visible handle and structured shell treatment"
    - path: "src/components/layout/__tests__/WorkspacePanels.test.tsx"
      provides: "workspace panel regression coverage"
      contains: "left rail, right panel, minimap, timeline assertions"
---

<objective>
Finish the workspace frame phase by collapsing the timeline by default and validating the new persistent panel structure.

Purpose: Phase 9 should end with a coherent workspace frame foundation that later graph-semantic and AI-routing phases can build on confidently.
Output: collapsed-by-default timeline, aligned command/timeline bands, and regression coverage for the new workspace structure.
</objective>

<context>
@.planning/phases/09-workspace-frame-panels/09-CONTEXT.md
@.planning/phases/09-workspace-frame-panels/09-RESEARCH.md
@.planning/phases/09-workspace-frame-panels/09-UI-SPEC.md
@.planning/phases/09-workspace-frame-panels/09-VALIDATION.md
@src/components/layout/CaseWorkspaceShell.tsx
@src/components/layout/TimelineBar.tsx
@src/components/layout/AICommandBar.tsx
@src/__tests__/app-shell.test.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Convert the timeline into a collapsed-by-default investigation band</name>
  <files>src/components/layout/CaseWorkspaceShell.tsx src/components/layout/TimelineBar.tsx</files>
  <action>
Refactor the timeline surface so it remains visibly present with a clear expand/collapse affordance while preserving the graph-first resting state and overall shell cohesion.
  </action>
  <verify>
    <automated>Workspace loads with a visible collapsed timeline band that can coexist with the graph and right panel layout</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 2: Align bottom bands and add workspace panel regression coverage</name>
  <files>src/components/layout/AICommandBar.tsx src/__tests__/app-shell.test.tsx src/components/layout/__tests__/WorkspacePanels.test.tsx</files>
  <action>
Ensure the command surface and collapsed timeline read as coordinated bottom bands, then add regression coverage for the new panel structure before running full validation.
  </action>
  <verify>
    <automated>Workspace panel tests pass and the full phase validation command succeeds</automated>
  </verify>
</task>

</tasks>

<success_criteria>
- Timeline is visibly present but collapsed by default
- Bottom shell bands remain coherent after the workspace frame rebuild
- Tests and full validation confirm Phase 9 is stable enough for later graph-focused phases
</success_criteria>

<output>
After completion, create `.planning/phases/09-workspace-frame-panels/09-03-SUMMARY.md`.
</output>
