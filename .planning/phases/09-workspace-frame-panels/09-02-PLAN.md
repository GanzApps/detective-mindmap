---
phase: 09-workspace-frame-panels
plan: 02
type: execute
wave: 2
depends_on:
  - 09-01-PLAN.md
files_modified:
  - src/components/layout/CaseWorkspaceShell.tsx
  - src/components/layout/WorkspaceAnalysisPanel.tsx
  - src/components/graph/GraphWorkspace.tsx
  - src/components/graph/NodeDetailPanel.tsx
  - src/components/graph/GraphMinimap.tsx
autonomous: true
requirements:
  - SHELL-08
  - WS-09
  - ANALYSIS-01
  - ANALYSIS-02

requirements_addressed:
  - SHELL-08
  - WS-09
  - ANALYSIS-01
  - ANALYSIS-02

must_haves:
  truths:
    - "The right analysis panel exists as a stable shell region rather than a floating graph overlay"
    - "The right panel remains visible in a quiet default state when nothing is selected"
    - "A minimap exists at the bottom-right of the graph area and remains visually quiet"
  artifacts:
    - path: "src/components/layout/WorkspaceAnalysisPanel.tsx"
      provides: "right-side shell-aligned analysis surface"
      contains: "idle and selected/result states"
    - path: "src/components/graph/GraphMinimap.tsx"
      provides: "persistent minimap anchor"
      contains: "bottom-right quiet viewport indicator"
---

<objective>
Add the right-side analysis surface and minimap so the workspace frame matches the reference structure.

Purpose: The workspace should gain a stable analysis region and orientation aid without obstructing the graph or dragging graph semantics into this phase.
Output: persistent right analysis panel plus quiet bottom-right minimap integrated into the workspace shell.
</objective>

<context>
@.planning/phases/09-workspace-frame-panels/09-CONTEXT.md
@.planning/phases/09-workspace-frame-panels/09-RESEARCH.md
@.planning/phases/09-workspace-frame-panels/09-UI-SPEC.md
@.planning/references/mac-ui-reference/graph-view-selected-filters.png
@src/components/layout/CaseWorkspaceShell.tsx
@src/components/graph/GraphWorkspace.tsx
@src/components/graph/NodeDetailPanel.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rebuild node/result analysis into a right-side workspace panel</name>
  <files>src/components/layout/CaseWorkspaceShell.tsx src/components/layout/WorkspaceAnalysisPanel.tsx src/components/graph/NodeDetailPanel.tsx src/components/graph/GraphWorkspace.tsx</files>
  <action>
Shift node analysis ownership from the current floating graph overlay into a right-side shell-aligned analysis panel that supports a quiet default idle state and active selected/result states.
  </action>
  <verify>
    <automated>Workspace renders a stable right analysis region in both idle and selected-node states</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 2: Introduce a persistent minimap anchor inside the graph frame</name>
  <files>src/components/graph/GraphWorkspace.tsx src/components/graph/GraphMinimap.tsx</files>
  <action>
Add a minimap to the bottom-right of the graph area with a low-noise shell treatment so the workspace gains constant orientation without pulling focus from the main graph.
  </action>
  <verify>
    <automated>Graph area renders an always-visible bottom-right minimap without breaking existing renderer switching</automated>
  </verify>
</task>

</tasks>

<success_criteria>
- Right-side analysis panel replaces the floating detail overlay as the primary detail surface
- The idle analysis state preserves layout stability without distracting from the graph
- Minimap is visible in the correct location and remains quiet
</success_criteria>

<output>
After completion, create `.planning/phases/09-workspace-frame-panels/09-02-SUMMARY.md`.
</output>
