---
phase: 09-workspace-frame-panels
plan: 01
type: execute
wave: 1
files_modified:
  - src/components/layout/CaseWorkspaceShell.tsx
  - src/components/layout/EvidenceSidebar.tsx
  - src/components/layout/WorkspaceFiltersPanel.tsx
  - src/components/graph/GraphWorkspace.tsx
autonomous: true
requirements:
  - SHELL-08
  - WS-07
  - WS-08

requirements_addressed:
  - SHELL-08
  - WS-07
  - WS-08

must_haves:
  truths:
    - "The workspace shell clearly separates left rail, graph hero surface, and bottom bands"
    - "The left rail defaults to Raw Evidence and exposes Filters & Layers as a true tabbed mode"
    - "Filter controls are grouped in the left rail rather than rendered inside the graph hero area"
  artifacts:
    - path: "src/components/layout/EvidenceSidebar.tsx"
      provides: "tabbed left rail"
      contains: "Raw Evidence and Filters & Layers modes"
    - path: "src/components/layout/WorkspaceFiltersPanel.tsx"
      provides: "grouped filter sections"
      contains: "entity types, connection layers, time range"
---

<objective>
Rebuild the left side of the workspace into the reference-aligned rail structure.

Purpose: Phase 9 should start by fixing the workspace composition so evidence navigation and filter controls live in the correct region before adding right-panel and timeline behavior.
Output: tabbed left rail with grouped filter controls and graph-area control clutter removed.
</objective>

<context>
@.planning/phases/09-workspace-frame-panels/09-CONTEXT.md
@.planning/phases/09-workspace-frame-panels/09-RESEARCH.md
@.planning/phases/09-workspace-frame-panels/09-UI-SPEC.md
@.planning/references/mac-ui-reference/graph-view-selected-filters.png
@src/components/layout/CaseWorkspaceShell.tsx
@src/components/layout/EvidenceSidebar.tsx
@src/components/graph/GraphWorkspace.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Refactor the left rail into tabbed workspace navigation</name>
  <files>src/components/layout/CaseWorkspaceShell.tsx src/components/layout/EvidenceSidebar.tsx</files>
  <action>
Restructure the workspace shell so the left rail becomes a real persistent band with tabbed modes, defaulting to Raw Evidence and preserving the shell token language introduced in Phase 8.
  </action>
  <verify>
    <automated>Workspace renders a stable left rail with Raw Evidence as the default visible tab</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 2: Extract grouped Filters & Layers content into its own panel</name>
  <files>src/components/layout/WorkspaceFiltersPanel.tsx src/components/layout/EvidenceSidebar.tsx src/components/graph/GraphWorkspace.tsx</files>
  <action>
Move the current graph control/filter surface out of the graph hero area and into a dedicated left-rail panel grouped into entity types, connection layers, and time range.
  </action>
  <verify>
    <automated>Graph-area filter clutter is removed and the grouped filter panel renders inside the left rail</automated>
  </verify>
</task>

</tasks>

<success_criteria>
- Left rail behaves as tabbed navigation rather than a single-purpose sidebar
- Grouped filters live in the left rail and match the approved section structure
- The graph hero area is visually cleaner because filter controls are no longer embedded inside it
</success_criteria>

<output>
After completion, create `.planning/phases/09-workspace-frame-panels/09-01-SUMMARY.md`.
</output>
