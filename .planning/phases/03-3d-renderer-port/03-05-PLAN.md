---
phase: 03-3d-renderer-port
plan: 05
type: execute
wave: 4
depends_on:
  - "03-04"
files_modified:
  - src/components/layout/CaseWorkspaceShell.tsx
  - src/components/pages/CaseWorkspacePage.tsx
  - src/app/cases/[caseId]/page.tsx
  - src/__tests__/mindmap3d.test.tsx
autonomous: true
requirements:
  - GRAPH3D-01
  - GRAPH3D-08
  - GRAPH3D-09

must_haves:
  truths:
    - "The live workspace route can render the new `MindMap3D` surface through the existing client-only boundary"
    - "The placeholder shell gives way to a real 3D renderer integration path"
    - "Build and tests prove the 3D renderer does not introduce SSR or lifecycle regressions"
  artifacts:
    - path: "src/components/pages/CaseWorkspacePage.tsx"
      provides: "Route-level 3D renderer integration"
      contains: "MindMap3D"
    - path: "src/__tests__/mindmap3d.test.tsx"
      provides: "Smoke coverage for the 3D component surface"
      contains: "MindMap3D"
---

<objective>
Integrate and prove the Phase 3 renderer inside the app shell.

Purpose: The 3D renderer must move from isolated component work into the actual case workspace without breaking the Phase 2 routing and client-boundary guarantees.
Output: live workspace integration plus verification coverage.
</objective>

<context>
@.planning/phases/03-3d-renderer-port/03-CONTEXT.md
@.planning/phases/02-app-shell/02-VERIFICATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace the placeholder graph surface with `MindMap3D` integration</name>
  <files>src/components/layout/CaseWorkspaceShell.tsx, src/components/pages/CaseWorkspacePage.tsx</files>
  <action>
Integrate the new 3D renderer into the current workspace shell so it uses live case graph data and shared selection state instead of the Phase 2 placeholder-only surface.
  </action>
  <verify>
    <automated>The workspace renders the 3D graph using actual case graph data</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 2: Preserve the client-only route boundary and shell stability</name>
  <files>src/app/cases/[caseId]/page.tsx</files>
  <action>
Keep the workspace route on the proven client-only path so the 3D renderer remains safe under Next.js and does not reintroduce SSR crashes.
  </action>
  <verify>
    <automated>`next build` succeeds with the live `MindMap3D` integration</automated>
  </verify>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add smoke coverage for the 3D component integration</name>
  <files>src/__tests__/mindmap3d.test.tsx</files>
  <action>
Add focused smoke coverage around the component surface, control rendering, or cleanup-sensitive wiring so the renderer has a regression guard beyond manual checks.
  </action>
  <verify>
    <automated>Jest covers the new 3D component surface without browser-only failures</automated>
  </verify>
</task>

</tasks>

<success_criteria>
- `MindMap3D` is wired into the real workspace route
- The route still builds cleanly with client-only boundaries
- Smoke coverage exists for the 3D renderer integration
</success_criteria>

<output>
After completion, create `.planning/phases/03-3d-renderer-port/03-05-SUMMARY.md`.
</output>
