---
phase: 03-3d-renderer-port
plan: 03
type: execute
wave: 2
depends_on:
  - "03-01"
  - "03-02"
files_modified:
  - src/components/graph/MindMap3D.tsx
autonomous: true
requirements:
  - GRAPH3D-01
  - GRAPH3D-08
  - GRAPH3D-09

must_haves:
  truths:
    - "A client-only `MindMap3D` Canvas component exists"
    - "Camera and interaction state live in refs rather than React re-render loops"
    - "Resize and DPR handling keep the canvas sharp and clean up correctly on unmount"
  artifacts:
    - path: "src/components/graph/MindMap3D.tsx"
      provides: "React Canvas component shell for the 3D renderer"
      contains: "use client"
---

<objective>
Build the 3D Canvas component skeleton around the extracted helpers.

Purpose: The renderer needs a stable React host with correct lifecycle, DPR handling, and strict-mode-safe cleanup before interactions are wired in.
Output: `MindMap3D` with refs, resize behavior, frame loop scaffolding, and cleanup.
</objective>

<context>
@.planning/phases/03-3d-renderer-port/03-CONTEXT.md
@src/store/caseStore.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the Canvas component shell with ref-owned renderer state</name>
  <files>src/components/graph/MindMap3D.tsx</files>
  <action>
Add a client component with a canvas ref and the camera, hover, drag, and animation state stored in refs so drag and render activity do not thrash React.
  </action>
  <verify>
    <automated>Component avoids state updates on every animation frame</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 2: Add resize and HiDPI handling</name>
  <files>src/components/graph/MindMap3D.tsx</files>
  <action>
Use `ResizeObserver` and device-pixel-ratio-aware sizing so the Canvas renders sharply and pointer math stays aligned across DPR values.
  </action>
  <verify>
    <automated>Canvas sizing uses DPR-aware dimensions and transform setup</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 3: Make the component strict-mode-safe</name>
  <files>src/components/graph/MindMap3D.tsx</files>
  <action>
Ensure animation loops, observers, and any event listeners are cleaned up correctly so React strict mode double-invocation does not leave stale loops behind.
  </action>
  <verify>
    <automated>Unmount path cancels rAF and disconnects observers/listeners</automated>
  </verify>
</task>

</tasks>

<success_criteria>
- `MindMap3D` exists as a client component
- Ref-based camera and animation state is in place
- Resize and DPR handling are wired correctly
- Cleanup is explicit and strict-mode-safe
</success_criteria>

<output>
After completion, create `.planning/phases/03-3d-renderer-port/03-03-SUMMARY.md`.
</output>
