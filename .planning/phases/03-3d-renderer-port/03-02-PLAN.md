---
phase: 03-3d-renderer-port
plan: 02
type: execute
wave: 1
depends_on:
  - "03-01"
files_modified:
  - src/lib/graph/renderer3d.ts
  - src/lib/graph/__tests__/renderer3d.test.ts
autonomous: true
requirements:
  - GRAPH3D-01
  - GRAPH3D-05
  - GRAPH3D-09

must_haves:
  truths:
    - "The frame draw logic is extracted into a stateless renderer helper"
    - "Depth ordering, glow, opacity, and broad label visibility follow the reference baseline"
    - "The draw helper is structured to avoid Canvas state leakage such as lingering globalAlpha"
  artifacts:
    - path: "src/lib/graph/renderer3d.ts"
      provides: "Stateless 3D frame drawing helper"
      contains: "drawFrame3D"
    - path: "src/lib/graph/__tests__/renderer3d.test.ts"
      provides: "Guardrail tests around render preparation or ordering logic"
      contains: "drawFrame3D"
---

<objective>
Extract the stateless render pass from the reference.

Purpose: Phase 3 needs the same visual language as the original renderer, but with clearer boundaries and no accidental Canvas state leaks.
Output: a `drawFrame3D` helper that prepares and draws a full frame from plain graph and camera input.
</objective>

<context>
@.planning/phases/03-3d-renderer-port/03-CONTEXT.md
@3d_mindmap_fixed.html
</context>

<tasks>

<task type="auto">
  <name>Task 1: Port the frame draw pipeline into a stateless helper</name>
  <files>src/lib/graph/renderer3d.ts</files>
  <action>
Move edge drawing, depth sorting, node glow, fill, outline, and label rendering into a helper that takes a canvas context plus plain renderer inputs. Preserve the visual fidelity decisions from the context.
  </action>
  <verify>
    <automated>The renderer helper can be called from a React component without owning app state itself</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 2: Eliminate Canvas state leakage from the draw loop</name>
  <files>src/lib/graph/renderer3d.ts</files>
  <action>
Wrap risky per-node or per-edge state changes defensively so alpha, stroke, shadows, and text styling do not leak into later draw steps.
  </action>
  <verify>
    <automated>Draw helper restores or explicitly resets temporary Canvas state changes</automated>
  </verify>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add narrow tests around render preparation logic</name>
  <files>src/lib/graph/__tests__/renderer3d.test.ts</files>
  <action>
Add lightweight tests around the pure pieces of render preparation such as sort order, visibility thresholds, or label/display preparation if the final draw function itself is too imperative to unit test directly.
  </action>
  <verify>
    <automated>Tests cover the extracted render-preparation behavior without DOM dependencies</automated>
  </verify>
</task>

</tasks>

<success_criteria>
- `drawFrame3D` exists in `src/lib/graph/renderer3d.ts`
- Visual preparation logic preserves reference-style depth and glow behavior
- Canvas state management is explicit and safe
</success_criteria>

<output>
After completion, create `.planning/phases/03-3d-renderer-port/03-02-SUMMARY.md`.
</output>
