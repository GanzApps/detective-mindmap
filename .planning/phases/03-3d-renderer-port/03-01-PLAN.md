---
phase: 03-3d-renderer-port
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/graph/projection3d.ts
  - src/lib/graph/hitTest3d.ts
  - src/lib/graph/__tests__/projection3d.test.ts
autonomous: true
requirements:
  - GRAPH3D-01
  - GRAPH3D-03
  - GRAPH3D-04

must_haves:
  truths:
    - "Pure projection and hit-test helpers exist outside React and DOM code"
    - "Projection math covers screen position, scale, and depth values needed by the renderer"
    - "Known-input tests prove projection and hit detection behavior"
  artifacts:
    - path: "src/lib/graph/projection3d.ts"
      provides: "Pure 3D projection helpers"
      contains: "projectNode"
    - path: "src/lib/graph/hitTest3d.ts"
      provides: "Pure 3D hit-testing helper"
      contains: "hitTest3D"
    - path: "src/lib/graph/__tests__/projection3d.test.ts"
      provides: "Node-only tests for 3D math"
      contains: "projectNode"
---

<objective>
Extract the reusable 3D math layer first.

Purpose: The renderer must preserve the reference behavior while remaining testable and portable. This plan creates the pure projection and hit-test functions that every later plan will depend on.
Output: standalone 3D math helpers with passing Jest tests.
</objective>

<context>
@.planning/phases/03-3d-renderer-port/03-CONTEXT.md
@3d_mindmap_fixed.html
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract projection helpers from the reference renderer</name>
  <files>src/lib/graph/projection3d.ts</files>
  <action>
Port the camera rotation and perspective logic from `3d_mindmap_fixed.html` into pure helpers that accept node coordinates and camera values, and return projected screen geometry plus depth information.
  </action>
  <verify>
    <automated>Projection helper is free of React and Canvas context imports</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 2: Extract 3D hit testing into a pure helper</name>
  <files>src/lib/graph/hitTest3d.ts</files>
  <action>
Create hit testing that uses projected node geometry and pointer coordinates to identify the hovered/selected node, preserving the feel of the reference interaction model.
  </action>
  <verify>
    <automated>Hit-test helper accepts plain data structures and returns a node id or null</automated>
  </verify>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add node-only tests for projection and hit testing</name>
  <files>src/lib/graph/__tests__/projection3d.test.ts</files>
  <action>
Add deterministic Jest tests using fixed nodes and camera values so later renderer work can trust the extracted math.
  </action>
  <verify>
    <automated>Jest passes for the new 3D math test suite</automated>
  </verify>
</task>

</tasks>

<success_criteria>
- `projectNode` and related helpers exist as pure functions
- `hitTest3D` exists as a pure function
- Tests validate expected projection and hit-test behavior
</success_criteria>

<output>
After completion, create `.planning/phases/03-3d-renderer-port/03-01-SUMMARY.md`.
</output>
