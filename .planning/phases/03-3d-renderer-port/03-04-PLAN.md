---
phase: 03-3d-renderer-port
plan: 04
type: execute
wave: 3
depends_on:
  - "03-03"
files_modified:
  - src/components/graph/MindMap3D.tsx
  - src/store/caseStore.ts
autonomous: true
requirements:
  - GRAPH3D-02
  - GRAPH3D-03
  - GRAPH3D-04
  - GRAPH3D-06
  - GRAPH3D-07

must_haves:
  truths:
    - "Drag rotates the graph and scroll adjusts zoom"
    - "Hover and click use hit testing to drive tooltip, selected-node emphasis, and panel data"
    - "Reset, pause/rotate, and labels controls work through the component surface"
  artifacts:
    - path: "src/components/graph/MindMap3D.tsx"
      provides: "Interactive 3D graph behavior"
      contains: "onWheel"
    - path: "src/store/caseStore.ts"
      provides: "Selected-node integration point"
      contains: "setSelectedNode"
---

<objective>
Wire the actual 3D interactions into the Canvas component.

Purpose: Phase 3 is only complete if the renderer behaves like the reference under drag, zoom, hover, selection, and control changes.
Output: fully interactive `MindMap3D` behavior tied to shared selection state.
</objective>

<context>
@.planning/phases/03-3d-renderer-port/03-CONTEXT.md
@3d_mindmap_fixed.html
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement drag and zoom interactions</name>
  <files>src/components/graph/MindMap3D.tsx</files>
  <action>
Port pointer drag rotation and wheel zoom from the reference so the scene feels the same, with smooth interaction prioritized over abstraction purity.
  </action>
  <verify>
    <automated>Pointer and wheel handlers update camera refs and redraw without React churn</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 2: Implement hover tooltip and selection behavior</name>
  <files>src/components/graph/MindMap3D.tsx, src/store/caseStore.ts</files>
  <action>
Use `hitTest3D` to drive hover state, tooltips, and click selection. Clicking should strongly emphasize the selected neighborhood in-canvas and send the selected node id into shared state for the surrounding shell.
  </action>
  <verify>
    <automated>Selected node ids can be consumed by the shared store and panel surface</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 3: Implement controls and panel behavior from the reference</name>
  <files>src/components/graph/MindMap3D.tsx</files>
  <action>
Expose reset, pause/rotate, and labels controls, plus the right-side detail panel behavior adapted into the component so it remains usable inside the app shell.
  </action>
  <verify>
    <automated>Reset, pause/rotate, and labels toggles all affect the rendered scene</automated>
  </verify>
</task>

</tasks>

<success_criteria>
- Drag and zoom match the reference interaction model
- Hover and click selection are wired
- Tooltip, panel, and control behavior all exist
- Selection updates shared app state
</success_criteria>

<output>
After completion, create `.planning/phases/03-3d-renderer-port/03-04-SUMMARY.md`.
</output>
