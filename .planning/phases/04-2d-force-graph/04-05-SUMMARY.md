# 04-05 Summary

- Added parent-controlled node search for the 2D graph through `CaseWorkspaceShell`.
- Search now highlights matches and auto-centers the best result without recreating the simulation.
- Added smoke coverage in `src/__tests__/forceGraph2d.test.tsx` and expanded `src/__tests__/app-shell.test.tsx`.
