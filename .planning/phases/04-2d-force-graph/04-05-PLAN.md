---
phase: 04-2d-force-graph
plan: 05
type: execute
wave: 4
depends_on:
  - "04-04"
files_modified:
  - src/components/graph/ForceGraph2D.tsx
  - src/components/layout/CaseWorkspaceShell.tsx
  - src/__tests__/forceGraph2d.test.tsx
  - src/__tests__/app-shell.test.tsx
autonomous: true
requirements:
  - GRAPH2D-09
  - GRAPH2D-10

must_haves:
  truths:
    - "Search behavior highlights matches and auto-centers the best result"
    - "Search and selection interactions do not restart the simulation"
    - "Smoke coverage protects the live 2D integration in the workspace shell"
  artifacts:
    - path: "src/components/graph/ForceGraph2D.tsx"
      provides: "Search-aware 2D graph behavior"
      contains: "search"
    - path: "src/__tests__/forceGraph2d.test.tsx"
      provides: "Smoke coverage for the 2D renderer surface"
      contains: "ForceGraph2D"
---

<objective>
Finish the 2D graph with search-centered navigation and regression coverage.

Purpose: The last Phase 4 step should make the graph navigable by search and prove that the final 2D integration does not regress the simulation lifecycle or workspace shell behavior.
Output: search-driven viewport centering plus smoke coverage for the live 2D renderer.
</objective>

<context>
@.planning/phases/04-2d-force-graph/04-CONTEXT.md
@src/components/layout/CaseWorkspaceShell.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add search-driven highlighting and best-match centering</name>
  <files>src/components/graph/ForceGraph2D.tsx, src/components/layout/CaseWorkspaceShell.tsx</files>
  <action>
Introduce controlled search input and connect it to the 2D graph so matches are highlighted and the best result is auto-centered in the current viewport.
  </action>
  <verify>
    <automated>Entering a matching node label updates highlight behavior and recenters the viewport on the strongest match</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 2: Prove search and UI-only updates do not restart the simulation</name>
  <files>src/components/graph/ForceGraph2D.tsx</files>
  <action>
Audit the component's effects and redraw paths so search, selection, and other UI-only state changes repaint the current simulation rather than recreate it.
  </action>
  <verify>
    <automated>The data-driven simulation effect is isolated from search and selection state inputs</automated>
  </verify>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add smoke coverage for the 2D renderer and workspace integration</name>
  <files>src/__tests__/forceGraph2d.test.tsx, src/__tests__/app-shell.test.tsx</files>
  <action>
Add focused smoke coverage around the 2D renderer shell and the workspace integration path so the live graph surface, search UI, and key controls have a regression guard.
  </action>
  <verify>
    <automated>Jest passes for the 2D renderer smoke coverage and updated shell expectations</automated>
  </verify>
</task>

</tasks>

<success_criteria>
- Search highlights matching nodes and auto-centers the best result
- Search and selection updates do not restart the simulation
- Smoke coverage protects the live 2D renderer integration path
</success_criteria>

<output>
After completion, create `.planning/phases/04-2d-force-graph/04-05-SUMMARY.md`.
</output>
