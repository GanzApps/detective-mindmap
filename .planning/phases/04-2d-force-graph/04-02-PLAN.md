---
phase: 04-2d-force-graph
plan: 02
type: execute
wave: 2
depends_on:
  - "04-01"
files_modified:
  - src/components/graph/ForceGraph2D.tsx
  - src/components/layout/CaseWorkspaceShell.tsx
autonomous: true
requirements:
  - GRAPH2D-01
  - GRAPH2D-10

must_haves:
  truths:
    - "A client-only `ForceGraph2D` component exists and owns the D3 simulation lifecycle in refs"
    - "Simulation startup and cleanup are isolated from React UI-only state changes"
    - "The workspace shell can render the new 2D surface in place of the placeholder"
  artifacts:
    - path: "src/components/graph/ForceGraph2D.tsx"
      provides: "Live 2D Canvas graph component"
      contains: "ForceGraph2D"
    - path: "src/components/layout/CaseWorkspaceShell.tsx"
      provides: "Workspace integration path for the 2D renderer"
      contains: "ForceGraph2D"
---

<objective>
Wire the pure 2D graph layer into a live Canvas component.

Purpose: Phase 4 needs a real renderer shell that owns simulation startup, ticking, redraw, and cleanup without letting React state churn restart the force layout.
Output: live `ForceGraph2D` component integrated into the workspace shell.
</objective>

<context>
@.planning/phases/04-2d-force-graph/04-CONTEXT.md
@src/components/graph/MindMap3D.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build the `ForceGraph2D` component skeleton with ref-owned lifecycle</name>
  <files>src/components/graph/ForceGraph2D.tsx</files>
  <action>
Create a client-only Canvas component that owns `canvasRef`, `simulationRef`, transform state refs, and redraw scheduling. Use `useEffect([nodes, edges])` only for data-driven simulation setup and ensure cleanup stops the simulation cleanly.
  </action>
  <verify>
    <automated>The simulation is created and stopped from effects without storing the D3 instance in React state</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 2: Separate redraws for selection and view changes from simulation re-creation</name>
  <files>src/components/graph/ForceGraph2D.tsx</files>
  <action>
Introduce redraw paths for selection, highlight, and viewport changes that repaint the current graph without tearing down and rebuilding the force simulation.
  </action>
  <verify>
    <automated>Selection and highlight props trigger redraw behavior without the data effect re-running</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 3: Replace the Phase 2 placeholder with the real 2D renderer path</name>
  <files>src/components/layout/CaseWorkspaceShell.tsx</files>
  <action>
Integrate `ForceGraph2D` into the existing workspace shell so the 2D mode shows a live graph using shared case graph data and shared node-selection callbacks.
  </action>
  <verify>
    <automated>The shell renders the live 2D graph when `viewMode` is `2d`</automated>
  </verify>
</task>

</tasks>

<success_criteria>
- `ForceGraph2D` exists as a client-only live Canvas surface
- The D3 simulation lifecycle is kept in refs and cleaned up explicitly
- The workspace shell uses the real 2D renderer path in place of the old placeholder
</success_criteria>

<output>
After completion, create `.planning/phases/04-2d-force-graph/04-02-SUMMARY.md`.
</output>
