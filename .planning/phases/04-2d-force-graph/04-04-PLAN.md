---
phase: 04-2d-force-graph
plan: 04
type: execute
wave: 3
depends_on:
  - "04-02"
  - "04-03"
files_modified:
  - src/components/graph/ForceGraph2D.tsx
  - src/lib/graph/forceSimulation.ts
  - src/store/caseStore.ts
autonomous: true
requirements:
  - GRAPH2D-04
  - GRAPH2D-07
  - GRAPH2D-08

must_haves:
  truths:
    - "Node drag repositions live nodes without collapsing click and drag into the same gesture"
    - "Node selection and deselection emit through the existing shared selection flow"
    - "Selected neighborhoods are emphasized while non-neighborhood nodes dim"
  artifacts:
    - path: "src/components/graph/ForceGraph2D.tsx"
      provides: "Drag, selection, and deselection interaction wiring"
      contains: "onSelectNode"
    - path: "src/lib/graph/forceSimulation.ts"
      provides: "Selection-aware 2D draw logic"
      contains: "selectedId"
---

<objective>
Complete the core graph interactions: drag, select, deselect, and neighborhood emphasis.

Purpose: Phase 4 is not just about showing nodes; it must support the core investigation workflow of repositioning the graph, selecting a node, and reading its direct network clearly.
Output: robust drag/select behavior integrated with the shared workspace state.
</objective>

<context>
@.planning/phases/04-2d-force-graph/04-CONTEXT.md
@.planning/research/PITFALLS.md
@src/store/caseStore.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement node drag with a threshold that preserves click behavior</name>
  <files>src/components/graph/ForceGraph2D.tsx</files>
  <action>
Wire D3 drag behavior with an explicit drag threshold so users can reposition nodes smoothly while still being able to click-select nodes without accidental drags.
  </action>
  <verify>
    <automated>Drag handling distinguishes click from drag and releases node constraints cleanly on drag end</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 2: Integrate selection and deselection with shared workspace state</name>
  <files>src/components/graph/ForceGraph2D.tsx, src/store/caseStore.ts</files>
  <action>
Emit selected node IDs through the same shared selection path used by the rest of the workspace, and clear selection when the user clicks empty canvas space.
  </action>
  <verify>
    <automated>Clicking a node selects it and clicking empty space clears it through the shared callback/store path</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 3: Apply neighborhood emphasis and non-selected dimming in the 2D draw layer</name>
  <files>src/lib/graph/forceSimulation.ts</files>
  <action>
Mirror the stronger selection emphasis direction established for Phase 3 by highlighting the selected node and direct edges while dimming unrelated graph elements.
  </action>
  <verify>
    <automated>The draw layer accepts selected/highlight inputs and renders different opacity or emphasis states for the active neighborhood</automated>
  </verify>
</task>

</tasks>

<success_criteria>
- Node drag is smooth and does not break click selection
- Selection and deselection flow through the shared workspace state
- The selected node neighborhood is visually emphasized in the live 2D graph
</success_criteria>

<output>
After completion, create `.planning/phases/04-2d-force-graph/04-04-SUMMARY.md`.
</output>
