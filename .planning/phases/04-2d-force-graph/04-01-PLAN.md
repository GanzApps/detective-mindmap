---
phase: 04-2d-force-graph
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/graph/forceSimulation.ts
  - src/lib/graph/__tests__/forceSimulation.test.ts
autonomous: true
requirements:
  - GRAPH2D-01
  - GRAPH2D-02
  - GRAPH2D-03

must_haves:
  truths:
    - "The 2D renderer's simulation setup, draw logic, and hit testing exist outside React code"
    - "Visual encoding for node types and selective edge-label rendering are defined in the pure draw layer"
    - "Deterministic Jest coverage protects the 2D math and rendering helpers before component wiring begins"
  artifacts:
    - path: "src/lib/graph/forceSimulation.ts"
      provides: "Pure D3 setup and 2D draw helpers"
      contains: "createForceSimulation"
    - path: "src/lib/graph/__tests__/forceSimulation.test.ts"
      provides: "Node-only coverage for force-graph helpers"
      contains: "createForceSimulation"
---

<objective>
Build the pure 2D graph foundation first.

Purpose: Phase 4 needs a stable testable core before any Canvas lifecycle or D3 DOM wiring begins. This plan extracts simulation setup, node/edge drawing, and hit testing into reusable helpers that later component work can trust.
Output: pure 2D force-graph helpers with focused Jest coverage.
</objective>

<context>
@.planning/phases/04-2d-force-graph/04-CONTEXT.md
@.planning/research/PITFALLS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create pure simulation and draw helpers for the 2D graph</name>
  <files>src/lib/graph/forceSimulation.ts</files>
  <action>
Implement `createForceSimulation(nodes, edges)`, `drawGraph2D(...)`, and `hitTest2D(...)` so the renderer's core behavior is defined in `src/lib/graph/` rather than inside a React component. Encode entity types visually and apply selective edge-label rendering by default to match the Phase 4 readability decision.
  </action>
  <verify>
    <automated>The helper module imports graph data types and D3 utilities only; no React imports</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 2: Capture the dark investigation-board drawing baseline</name>
  <files>src/lib/graph/forceSimulation.ts</files>
  <action>
Define the 2D node, edge, label, and background styling in the pure draw layer so the first 2D pass already feels visually aligned with the existing workspace and the dark reference direction.
  </action>
  <verify>
    <automated>The draw helper exposes enough styling logic to support color-coded nodes, readable labels, and selective relationship text</automated>
  </verify>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add deterministic tests for simulation helpers and hit testing</name>
  <files>src/lib/graph/__tests__/forceSimulation.test.ts</files>
  <action>
Add Jest coverage around the pure helper layer, including force setup shape, draw-preparation assumptions, and hit-test behavior using fixed graph fixtures and viewport inputs.
  </action>
  <verify>
    <automated>Jest passes for the new force-graph helper suite</automated>
  </verify>
</task>

</tasks>

<success_criteria>
- Pure 2D simulation, draw, and hit-test helpers exist in `src/lib/graph/`
- Visual encoding for node types and selective edge-label rendering are defined before component wiring
- Jest protects the helper layer with deterministic inputs
</success_criteria>

<output>
After completion, create `.planning/phases/04-2d-force-graph/04-01-SUMMARY.md`.
</output>
