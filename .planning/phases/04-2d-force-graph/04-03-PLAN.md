---
phase: 04-2d-force-graph
plan: 03
type: execute
wave: 2
depends_on:
  - "04-02"
files_modified:
  - src/components/graph/ForceGraph2D.tsx
  - src/lib/graph/forceSimulation.ts
autonomous: true
requirements:
  - GRAPH2D-03
  - GRAPH2D-05
  - GRAPH2D-06

must_haves:
  truths:
    - "Pan and zoom behavior exist on the live 2D graph with bounded interaction"
    - "Zoom to Fit is available and uses the current graph bounds"
    - "Selective edge-label rendering remains readable while the graph is moving"
  artifacts:
    - path: "src/components/graph/ForceGraph2D.tsx"
      provides: "Viewport and interaction handling for the 2D graph"
      contains: "Zoom to Fit"
    - path: "src/lib/graph/forceSimulation.ts"
      provides: "Transform-aware draw logic"
      contains: "drawGraph2D"
---

<objective>
Add viewport controls and readable relationship rendering.

Purpose: The 2D graph must be navigable at workspace scale, and the visual output needs to preserve relationship readability without collapsing into a cluttered canvas.
Output: bounded pan/zoom behavior, Zoom to Fit, and transform-aware edge-label rendering.
</objective>

<context>
@.planning/phases/04-2d-force-graph/04-CONTEXT.md
@image.png
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement transform-aware pan and zoom behavior</name>
  <files>src/components/graph/ForceGraph2D.tsx</files>
  <action>
Wire D3 zoom behavior into the Canvas surface using ref-owned transform state so users can pan empty space and zoom with the wheel without destabilizing the simulation.
  </action>
  <verify>
    <automated>The current transform updates redraws without rebuilding the simulation</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 2: Add Zoom to Fit based on the current graph bounds</name>
  <files>src/components/graph/ForceGraph2D.tsx, src/lib/graph/forceSimulation.ts</files>
  <action>
Provide a Zoom to Fit control that computes the current graph bounds and resets the viewport to show the full graph comfortably within the workspace canvas.
  </action>
  <verify>
    <automated>The component exposes a working Zoom to Fit control and applies a transform reset based on graph bounds</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 3: Keep edge labels selectively visible under transform and motion</name>
  <files>src/lib/graph/forceSimulation.ts</files>
  <action>
Apply the Phase 4 label-density decision in the draw layer so relationship labels remain readable and intentional while the graph is zoomed, panned, or still settling.
  </action>
  <verify>
    <automated>Draw logic filters or gates relationship labels rather than rendering all of them unconditionally</automated>
  </verify>
</task>

</tasks>

<success_criteria>
- Users can pan and zoom the 2D graph smoothly
- Zoom to Fit exists and resets the viewport to the current graph bounds
- Edge labels remain selectively visible and readable rather than overwhelming the canvas
</success_criteria>

<output>
After completion, create `.planning/phases/04-2d-force-graph/04-03-SUMMARY.md`.
</output>
