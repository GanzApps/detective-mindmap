# 04-01 Summary

- Added `src/lib/graph/forceSimulation.ts` with pure 2D helpers for force simulation setup, bounds calculation, hit testing, search matching, and transform-aware drawing.
- Encoded entity types visually with node color and glyph handling, plus selective relationship-label rendering by default.
- Added deterministic coverage in `src/lib/graph/__tests__/forceSimulation.test.ts`.
