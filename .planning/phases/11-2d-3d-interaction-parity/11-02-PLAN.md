# Phase 11-02 Plan: Interaction Parity

## Goal

Align gesture behavior so the user can switch between renderers without relearning the workspace.

## Tasks

1. Audit current click/drag behavior in both renderers.
2. Ensure node selection is consistent in both views.
3. Ensure dragging a node moves the node in both views.
4. Ensure navigating empty space remains distinct from node manipulation.
5. Keep scroll zoom local to the active graph surface.

## Files

- `src/components/graph/ForceGraph2D.tsx`
- `src/components/graph/MindMap3D.tsx`
- `src/lib/graph/hitTest3d.ts`

## Done When

- gesture intent is aligned across 2D and 3D
- no renderer-specific control regression is introduced
