---
phase: 08-design-tokens-shell-alignment
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/globals.css
  - src/app/layout.tsx
  - tailwind.config.ts
autonomous: true
requirements:
  - SHELL-06
  - THEME-01

requirements_addressed:
  - SHELL-06
  - THEME-01

must_haves:
  truths:
    - "Shared shell tokens exist centrally instead of being hardcoded per page"
    - "Light and dark themes are supported by the token layer from the start"
    - "Shell tokens stay distinct from graph/entity semantic colors"
  artifacts:
    - path: "src/app/globals.css"
      provides: "Centralized theme and shell token definitions"
      contains: "CSS variables"
    - path: "src/app/layout.tsx"
      provides: "App-level theme hook-in point"
      contains: "root shell theme contract"
---

<objective>
Create the token and theme foundation for the revamp shell.

Purpose: Later shell and workspace refactors need one stable source of truth for background, surface, text, spacing, elevation, and accent values rather than per-component dark styling.
Output: centralized shell token layer with light/dark support wired at the app root.
</objective>

<context>
@.planning/phases/08-design-tokens-shell-alignment/08-CONTEXT.md
@.planning/phases/08-design-tokens-shell-alignment/08-RESEARCH.md
@.planning/phases/08-design-tokens-shell-alignment/08-UI-SPEC.md
@src/app/globals.css
@src/app/layout.tsx
@tailwind.config.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Introduce centralized shell tokens</name>
  <files>src/app/globals.css</files>
  <action>
Define CSS variables for shell backgrounds, surfaces, text hierarchy, borders, accent, destructive color, spacing, radius, and elevation so the dashboard and workspace can share one visual system.
  </action>
  <verify>
    <automated>Token definitions exist centrally and can be consumed by multiple shell surfaces</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 2: Wire light and dark theme entry points</name>
  <files>src/app/layout.tsx src/app/globals.css</files>
  <action>
Establish the app-level theme contract so both light and dark modes can be applied without forcing graph entity colors into the shell token palette.
  </action>
  <verify>
    <automated>App shell can resolve both theme modes through the shared token layer</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 3: Align Tailwind usage with the new token model</name>
  <files>tailwind.config.ts src/app/globals.css</files>
  <action>
Adjust the styling foundation so existing Tailwind utility usage can consume or coexist with the new shell token system cleanly during later refactors.
  </action>
  <verify>
    <automated>Tailwind-based shell surfaces can reference the shared token model without build regressions</automated>
  </verify>
</task>

</tasks>

<success_criteria>
- Shared shell tokens are centralized
- Light and dark theme entry points exist
- The shell token system is ready for dashboard/workspace refactors
</success_criteria>

<output>
After completion, create `.planning/phases/08-design-tokens-shell-alignment/08-01-SUMMARY.md`.
</output>
