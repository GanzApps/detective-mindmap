---
phase: 08-design-tokens-shell-alignment
plan: 03
type: execute
wave: 3
depends_on:
  - 08-01-PLAN.md
  - 08-02-PLAN.md
files_modified:
  - src/components/pages/CaseListPage.tsx
  - src/components/layout/CaseWorkspaceShell.tsx
  - src/components/layout/CaseHeader.tsx
  - src/__tests__/app-shell.test.tsx
  - src/components/layout/__tests__/CaseHeader.test.tsx
autonomous: true
requirements:
  - SHELL-06
  - THEME-01

requirements_addressed:
  - SHELL-06
  - THEME-01

must_haves:
  truths:
    - "The new shell works in both light and dark themes"
    - "Shared shell styling does not regress dashboard/workspace route behavior"
    - "Shell-level tests and builds verify the Phase 8 foundation before later UI phases"
  artifacts:
    - path: "src/__tests__/app-shell.test.tsx"
      provides: "Shell regression coverage"
      contains: "app shell expectations"
    - path: "src/components/layout/__tests__/CaseHeader.test.tsx"
      provides: "Header regression coverage"
      contains: "top action behavior"
---

<objective>
Apply, verify, and harden the shell token system across the current entry surfaces.

Purpose: Phase 8 should end with a usable shell foundation in both themes and enough verification coverage that later UI phases can build on it confidently.
Output: theme-aware shell applied across list/workspace surfaces with tests and build verification ready for execution.
</objective>

<context>
@.planning/phases/08-design-tokens-shell-alignment/08-CONTEXT.md
@.planning/phases/08-design-tokens-shell-alignment/08-RESEARCH.md
@.planning/phases/08-design-tokens-shell-alignment/08-UI-SPEC.md
@.planning/phases/08-design-tokens-shell-alignment/08-VALIDATION.md
@src/components/pages/CaseListPage.tsx
@src/components/layout/CaseWorkspaceShell.tsx
@src/components/layout/CaseHeader.tsx
@src/__tests__/app-shell.test.tsx
@src/components/layout/__tests__/CaseHeader.test.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Apply final theme-aware shell styling</name>
  <files>src/components/pages/CaseListPage.tsx src/components/layout/CaseWorkspaceShell.tsx src/components/layout/CaseHeader.tsx</files>
  <action>
Finish the shell-level adoption of the new token system so both dashboard and workspace read cleanly in light and dark modes without conflicting with the later graph-specific visual work.
  </action>
  <verify>
    <automated>Shell surfaces render with theme-aware styling while preserving current route behavior</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 2: Update shell-level regression tests</name>
  <files>src/__tests__/app-shell.test.tsx src/components/layout/__tests__/CaseHeader.test.tsx</files>
  <action>
Adjust existing tests so they verify the shell foundation still renders correctly and preserves the key top-level interactions after the layout/token refactor.
  </action>
  <verify>
    <automated>Targeted shell tests pass against the refactored shell surfaces</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 3: Run full Phase 8 validation checks</name>
  <files>src/components/pages/CaseListPage.tsx src/components/layout/CaseWorkspaceShell.tsx src/components/layout/CaseHeader.tsx</files>
  <action>
Run the phase validation commands and confirm the shell foundation is stable enough for the later panel, graph, and command-surface phases to build on top of it.
  </action>
  <verify>
    <automated>`pnpm exec tsc --noEmit && pnpm test -- --runInBand && pnpm build` passes after the shell/theme refactor</automated>
  </verify>
</task>

</tasks>

<success_criteria>
- Shell styling works in both light and dark modes
- Shell-level regressions are covered by tests
- Phase 8 finishes with a verified shell foundation for later revamp phases
</success_criteria>

<output>
After completion, create `.planning/phases/08-design-tokens-shell-alignment/08-03-SUMMARY.md`.
</output>
