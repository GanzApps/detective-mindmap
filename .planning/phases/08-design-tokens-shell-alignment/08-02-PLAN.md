---
phase: 08-design-tokens-shell-alignment
plan: 02
type: execute
wave: 2
depends_on:
  - 08-01-PLAN.md
files_modified:
  - src/components/pages/CaseListPage.tsx
  - src/components/pages/CaseWorkspacePage.tsx
  - src/components/layout/CaseWorkspaceShell.tsx
  - src/components/layout/CaseHeader.tsx
autonomous: true
requirements:
  - SHELL-06

requirements_addressed:
  - SHELL-06

must_haves:
  truths:
    - "Dashboard and workspace share the same restrained shell framing"
    - "Top-level navigation and action areas feel consistent across list and workspace views"
    - "The graph remains the hero surface but no longer sits in a raw full-bleed shell"
  artifacts:
    - path: "src/components/pages/CaseListPage.tsx"
      provides: "Reference-aligned dashboard shell"
      contains: "centered layout framing"
    - path: "src/components/layout/CaseWorkspaceShell.tsx"
      provides: "Reference-aligned workspace shell"
      contains: "shared shell composition"
---

<objective>
Refactor the list and workspace shell surfaces to use the new reference-aligned framing.

Purpose: The dashboard and workspace should read as one product family before later phases rebuild the workspace internals.
Output: shared shell structure across the case list and investigation workspace entry surfaces.
</objective>

<context>
@.planning/phases/08-design-tokens-shell-alignment/08-CONTEXT.md
@.planning/phases/08-design-tokens-shell-alignment/08-RESEARCH.md
@.planning/phases/08-design-tokens-shell-alignment/08-UI-SPEC.md
@.planning/notes/ui-reference-mac-video-alignment.md
@src/components/pages/CaseListPage.tsx
@src/components/pages/CaseWorkspacePage.tsx
@src/components/layout/CaseWorkspaceShell.tsx
@src/components/layout/CaseHeader.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reframe the case list page into the new shell language</name>
  <files>src/components/pages/CaseListPage.tsx</files>
  <action>
Replace the current heavy dark hero styling with the centered, calmer dashboard framing that matches the saved reference while preserving the case list's current information architecture.
  </action>
  <verify>
    <automated>Case list renders inside the new restrained shell without losing case navigation behavior</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 2: Reframe the workspace page and shell containers</name>
  <files>src/components/pages/CaseWorkspacePage.tsx src/components/layout/CaseWorkspaceShell.tsx</files>
  <action>
Update the page-level workspace framing so the graph and persistent bands live inside a structured product shell aligned with the dashboard rather than a standalone dark tool layout.
  </action>
  <verify>
    <automated>Workspace route uses the same shell language as the dashboard while keeping graph functionality intact</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 3: Align header and top actions with the new shell system</name>
  <files>src/components/layout/CaseHeader.tsx</files>
  <action>
Restyle the header/action surface so it matches the reference language for top actions, spacing, and accent usage while preserving existing controls and export entry points.
  </action>
  <verify>
    <automated>Header actions remain functional and visually aligned with the new shell system</automated>
  </verify>
</task>

</tasks>

<success_criteria>
- Dashboard and workspace share the same shell framing
- Header and action areas feel consistent across entry points
- The graph remains the hero surface inside a calmer product shell
</success_criteria>

<output>
After completion, create `.planning/phases/08-design-tokens-shell-alignment/08-02-SUMMARY.md`.
</output>
