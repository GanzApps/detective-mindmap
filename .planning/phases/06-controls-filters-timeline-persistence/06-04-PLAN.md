---
phase: 06-controls-filters-timeline-persistence
plan: 04
type: execute
wave: 4
depends_on:
  - "06-01"
  - "06-02"
  - "06-03"
files_modified:
  - src/store/caseStore.ts
  - src/__tests__/app-shell.test.tsx
  - src/components/layout/__tests__/TimelineBar.test.tsx
  - src/components/layout/__tests__/AICommandBar.test.tsx
  - src/components/graph/__tests__/GraphWorkspace.test.tsx
autonomous: true
requirements:
  - DATA-04
  - SHELL-05
  - WS-05
  - WS-06
  - ARCH-05

requirements_addressed:
  - DATA-04
  - SHELL-05
  - WS-05
  - WS-06
  - ARCH-05

must_haves:
  truths:
    - "Case and workspace UI state restore from localStorage on reload"
    - "Phase 6 controls and footer surfaces are covered by focused regression tests"
    - "Verification proves filters, layers, footer UI, and persistence work together"
  artifacts:
    - path: "src/store/caseStore.ts"
      provides: "Persisted shared investigation state"
      contains: "persist"
    - path: "src/components/layout/__tests__/TimelineBar.test.tsx"
      provides: "Footer component coverage"
      contains: "TimelineBar"
---

<objective>
Close Phase 6 with persistence and regression coverage.

Purpose: The new controls and footer only become trustworthy when they survive reloads and are protected by tests.
Output: persisted workspace state plus focused test coverage and verification updates.
</objective>

<context>
@.planning/phases/06-controls-filters-timeline-persistence/06-CONTEXT.md
@src/store/caseStore.ts
@src/__tests__/app-shell.test.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add localStorage persistence to the shared store</name>
  <files>src/store/caseStore.ts</files>
  <action>
Use Zustand persistence so case mutations and practical workspace state restore on reload, including selection, filters, view mode, and layer preferences.
  </action>
  <verify>
    <automated>Store rehydrates persisted case and workspace state from localStorage</automated>
  </verify>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add focused tests for the new footer and control behavior</name>
  <files>src/components/layout/__tests__/TimelineBar.test.tsx, src/components/layout/__tests__/AICommandBar.test.tsx, src/components/graph/__tests__/GraphWorkspace.test.tsx, src/__tests__/app-shell.test.tsx</files>
  <action>
Add or update RTL/Jest coverage so filters, layer toggles, footer surfaces, and persistence-sensitive shell behavior are protected by regression tests.
  </action>
  <verify>
    <automated>Tests assert footer rendering, workspace controls, and persisted-state behavior</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 3: Run full verification and record Phase 6 completion evidence</name>
  <files>.planning/phases/06-controls-filters-timeline-persistence/*</files>
  <action>
Run the phase verification loop after implementation, then write the phase summaries and verification artifact documenting that controls, footer UI, and persistence landed together successfully.
  </action>
  <verify>
    <automated>`tsc`, `jest`, and `next build` all pass for the completed Phase 6 implementation</automated>
  </verify>
</task>

</tasks>

<success_criteria>
- Shared store persistence restores case and UI state on reload
- Focused regression coverage exists for footer and control behavior
- Phase verification proves the integrated Phase 6 experience works end to end
</success_criteria>

<output>
After completion, create `.planning/phases/06-controls-filters-timeline-persistence/06-04-SUMMARY.md`.
</output>
