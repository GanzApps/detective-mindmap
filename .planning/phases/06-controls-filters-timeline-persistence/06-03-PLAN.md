---
phase: 06-controls-filters-timeline-persistence
plan: 03
type: execute
wave: 3
depends_on:
  - "06-01"
  - "06-02"
files_modified:
  - src/components/layout/CaseWorkspaceShell.tsx
  - src/components/layout/TimelineBar.tsx
  - src/components/layout/AICommandBar.tsx
autonomous: true
requirements:
  - SHELL-05

requirements_addressed:
  - SHELL-05

must_haves:
  truths:
    - "The workspace footer reads as a strong investigation-board status bar rather than a generic app footer"
    - "A TimelineBar and AI command placeholder render at the bottom of the workspace"
    - "The footer integrates current case and selection context without expanding into full timeline analytics"
  artifacts:
    - path: "src/components/layout/TimelineBar.tsx"
      provides: "Visual timeline/status footer surface"
      contains: "TimelineBar"
    - path: "src/components/layout/AICommandBar.tsx"
      provides: "AI command placeholder surface"
      contains: "AICommandBar"
---

<objective>
Add the Phase 6 footer surfaces.

Purpose: The workspace needs the bottom-of-screen timeline/status treatment promised in the roadmap, but kept intentionally lightweight and visually strong for this pass.
Output: a footer timeline/status bar with AI command placeholder integrated into the case shell.
</objective>

<context>
@.planning/phases/06-controls-filters-timeline-persistence/06-CONTEXT.md
@image.png
@src/components/layout/CaseWorkspaceShell.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build the visual TimelineBar footer surface</name>
  <files>src/components/layout/TimelineBar.tsx</files>
  <action>
Create a presentational timeline/status bar that reflects the current case and investigation context, includes a placeholder scrubber, and matches the dark board aesthetic without introducing rich event logic yet.
  </action>
  <verify>
    <automated>TimelineBar renders case-aware footer content and placeholder scrubber UI</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 2: Build the AI command placeholder input surface</name>
  <files>src/components/layout/AICommandBar.tsx</files>
  <action>
Create a styled but non-functional AI command bar placeholder that looks intentional in the workspace footer and clearly reads as future capability rather than dead UI.
  </action>
  <verify>
    <automated>AICommandBar renders placeholder affordance and helper copy without business logic</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 3: Integrate the footer into the case workspace shell</name>
  <files>src/components/layout/CaseWorkspaceShell.tsx</files>
  <action>
Replace or evolve the existing lower shell section so TimelineBar and AICommandBar sit beneath the graph area as the Phase 6 footer/status strip.
  </action>
  <verify>
    <automated>CaseWorkspaceShell renders the new footer surfaces at the bottom of the workspace</automated>
  </verify>
</task>

</tasks>

<success_criteria>
- TimelineBar exists as a strong visual footer/status bar
- AI command placeholder exists in the footer
- The case shell presents both surfaces as part of the investigation workspace, not an unrelated footer
</success_criteria>

<output>
After completion, create `.planning/phases/06-controls-filters-timeline-persistence/06-03-SUMMARY.md`.
</output>
