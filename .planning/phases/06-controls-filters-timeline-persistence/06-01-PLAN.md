---
phase: 06-controls-filters-timeline-persistence
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/store/caseStore.ts
  - src/components/graph/GraphWorkspace.tsx
  - src/components/layout/CaseWorkspaceShell.tsx
autonomous: true
requirements:
  - WS-05
  - WS-06
  - ARCH-03

requirements_addressed:
  - WS-05
  - WS-06
  - ARCH-03

must_haves:
  truths:
    - "Shared filter and layer state exists in the Zustand store rather than renderer-local state"
    - "GraphWorkspace exposes a dedicated controls surface for filter and layer management"
    - "CaseWorkspaceShell delegates the graph-area controls to GraphWorkspace cleanly"
  artifacts:
    - path: "src/store/caseStore.ts"
      provides: "Filter and layer preference state"
      contains: "activeFilters"
    - path: "src/components/graph/GraphWorkspace.tsx"
      provides: "Shared controls shell for filter and layer actions"
      contains: "FilterPanel"
---

<objective>
Create the shared control-state foundation for Phase 6.

Purpose: Filters and layer toggles must live in the shared workspace state before either renderer can honor them consistently.
Output: Zustand-backed filter/layer state and a graph-workspace control surface wired into the existing shell.
</objective>

<context>
@.planning/phases/06-controls-filters-timeline-persistence/06-CONTEXT.md
@src/store/caseStore.ts
@src/components/graph/GraphWorkspace.tsx
@src/components/layout/CaseWorkspaceShell.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend Zustand with filter and layer preferences</name>
  <files>src/store/caseStore.ts</files>
  <action>
Add shared state for entity-type filters and layer toggles, including edge labels, node labels, and focus-selected-neighborhood behavior, so all graph surfaces can consume one source of truth.
  </action>
  <verify>
    <automated>Store exports and selectors cover filter and layer preference state</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 2: Build a workspace control shell for filters and layers</name>
  <files>src/components/graph/GraphWorkspace.tsx</files>
  <action>
Add a compact control surface inside GraphWorkspace for filter chips/checkboxes and layer toggles that fits the dark investigation-board styling and uses the new shared store-backed state.
  </action>
  <verify>
    <automated>GraphWorkspace renders filter and layer controls from shared state</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 3: Keep CaseWorkspaceShell focused on layout-level composition</name>
  <files>src/components/layout/CaseWorkspaceShell.tsx</files>
  <action>
Wire the shell to the updated GraphWorkspace contract without reintroducing graph-specific state or control duplication at the shell level.
  </action>
  <verify>
    <automated>CaseWorkspaceShell passes graph-level control concerns into GraphWorkspace instead of owning them directly</automated>
  </verify>
</task>

</tasks>

<success_criteria>
- Shared filter and layer state is available from Zustand
- GraphWorkspace renders the Phase 6 control surface
- CaseWorkspaceShell stays focused on higher-level case layout concerns
</success_criteria>

<output>
After completion, create `.planning/phases/06-controls-filters-timeline-persistence/06-01-SUMMARY.md`.
</output>
