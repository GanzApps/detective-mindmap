---
phase: 06-controls-filters-timeline-persistence
plan: 02
type: execute
wave: 2
depends_on:
  - "06-01"
files_modified:
  - src/components/graph/GraphWorkspace.tsx
  - src/components/graph/ForceGraph2D.tsx
  - src/components/graph/MindMap3D.tsx
  - src/lib/graph/forceSimulation.ts
  - src/lib/graph/renderer3d.ts
autonomous: true
requirements:
  - WS-05
  - WS-06

requirements_addressed:
  - WS-05
  - WS-06

must_haves:
  truths:
    - "Entity-type filters dim non-matching nodes instead of removing them from the scene"
    - "Both renderers respond to shared label and focus-neighborhood layer toggles"
    - "Applying filters or layer changes does not recreate the D3 simulation or reset the 3D camera"
  artifacts:
    - path: "src/components/graph/ForceGraph2D.tsx"
      provides: "2D renderer reaction to filters and layer toggles"
      contains: "selectedNodeId"
    - path: "src/components/graph/MindMap3D.tsx"
      provides: "3D renderer reaction to filters and layer toggles"
      contains: "showLabels"
---

<objective>
Wire shared filter and layer behavior into both renderers.

Purpose: Phase 6 only works if the 2D and 3D graphs honor the same controls without losing continuity or rebuilding expensive renderer state.
Output: both renderers support dimming-based filters and shared layer toggles.
</objective>

<context>
@.planning/phases/06-controls-filters-timeline-persistence/06-CONTEXT.md
@src/components/graph/GraphWorkspace.tsx
@src/components/graph/ForceGraph2D.tsx
@src/components/graph/MindMap3D.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Derive filtered graph emphasis in GraphWorkspace</name>
  <files>src/components/graph/GraphWorkspace.tsx</files>
  <action>
Compute the renderer-facing filter and focus state in GraphWorkspace so both renderers receive the same active entity types, dimming behavior, and focus-selected-neighborhood flag.
  </action>
  <verify>
    <automated>GraphWorkspace passes shared filter/layer props into both renderers</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 2: Update ForceGraph2D to honor dimming filters and layer toggles</name>
  <files>src/components/graph/ForceGraph2D.tsx, src/lib/graph/forceSimulation.ts</files>
  <action>
Extend the 2D draw path so non-matching nodes dim instead of disappearing, edge/node labels follow the layer toggles, and neighborhood focus intensifies the selected subgraph without restarting the simulation.
  </action>
  <verify>
    <automated>2D renderer redraws correctly for filters and layers without rebuilding the simulation</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 3: Update MindMap3D to honor dimming filters and layer toggles</name>
  <files>src/components/graph/MindMap3D.tsx, src/lib/graph/renderer3d.ts</files>
  <action>
Extend the 3D draw path so the same filter and layer state controls dimming, label visibility, and selection-neighborhood emphasis while preserving camera continuity.
  </action>
  <verify>
    <automated>3D renderer redraws correctly for filters and layers without resetting camera state</automated>
  </verify>
</task>

</tasks>

<success_criteria>
- Both renderers honor the same dimming-based filter behavior
- Layer toggles affect labels and focus mode consistently across 2D and 3D
- Renderer continuity is preserved while controls change
</success_criteria>

<output>
After completion, create `.planning/phases/06-controls-filters-timeline-persistence/06-02-SUMMARY.md`.
</output>
